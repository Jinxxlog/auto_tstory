# 글담 개발 로드맵 2차

작성일: 2026-09-11 · 개정일: 2026-09-23

상태: P0~P4 핵심·R0 기반·R1 판정 완료. R0.1·R7.1 구현·로컬 검증 완료(실제 티스토리 재확인·타 PC·원격 CI 미실행). R2 10장 실제 비공개 전송과 R3 구현·합성 자료의 실제 생성/편집/비공개 전송 인수 완료. 실제 사용자 자료·문체 품질은 별도 대기. 다음 제품 단계는 R5이며 R6·R7.2/7.3은 계획 단계다.

## 1. 제품 방향

**사진과 메모, 참고 자료를 모아 내 문체의 글을 만들고 티스토리·네이버 블로그에 게시하는 개인용 로컬 웹앱.**

기존 프로젝트·기술·PS 글을 유지하면서 여행·정보·자유 글을 추가한다. AI는 ChatGPT를 유지하고, R1에서 보류한 Gemini 소비자 로그인 대신 수동 결과 가져오기를 선택 확장으로 검토한다. 원고를 만든 AI와 발행할 블로그는 서로 독립적으로 선택한다.

이번 문서는 1차 계획의 미완료 P5·P6와 기존 R0~R7을 유지하면서 [배포 준비 점검](docs/DEPLOYMENT_READINESS_2026-09-22.md)의 복구·설치 문제를 반영한다. 실제 구현·검증 이력의 기준은 [PROJECT_PLAN.md](PROJECT_PLAN.md)다. 완료 체크는 실측한 기존 결과이며, 미체크 항목은 계획이다. 계획 수립만으로 공개 발행, 새 계정 로그인, 유료 서비스 결제·설치가 실행되는 것은 아니다.

첫 배포 대상은 **개인용 Windows 로컬 앱**이다. R5에서 한 사용자가 티스토리·네이버 연결을 따로 관리하되 전송은 직렬 처리한다. 서버형 다중 사용자 서비스, 결제, 대량 무인 발행은 이번 출시 범위에 포함하지 않는다. 기본값은 계속 비공개이며 R6 이후에도 사용자가 대상·원고 버전·공개 범위를 확인한다.

## 2. 현재 가능한 것과 남은 것

| 영역 | 현재 구현·검증 | 남은 작업과 2차 배치 |
| --- | --- | --- |
| 티스토리 | 개인 블로그 1개, 사진 10장 중간 배치·비공개 저장/재열기·코드·표 확인 | 공개 설정, 다른 계정/스킨 대응 → R6·R7.2 |
| 원고·사진 | 블록 순서·본문 사이 사진, 메모/캡션/긴 설명 분리, 썸네일, 50장 업로드·재시도, 과거 버전 보존 로컬 검증 | 실제 사용자 고해상도 사진·다른 PC 성능 → R7.2 |
| AI | 여섯 유형·개요 검토·ID 재작성·AI 잠금, 새 3유형 실제 생성/재작성 검증 | 선택 사진 분석 캐시 실제 AI, Gemini 수동 결과 가져오기 → R4 |
| 네이버 | 전용 로그인, 문단·사진·캡션 교차 입력, 대표 사진·카테고리·비공개 저장·재열기 실험 성공 | 조건부 어댑터와 플랫폼별 이력 → R5 |
| 문체 | 여섯 유형·공통 문체, 블로그/유형 기본값·글별 선택·스냅샷/백업 | 실제 사용자 새 유형 문체 품질, 네이버 원문 가져오기 → R5 |
| 기술·PS | 참고 자료, 해설·예제·검증 수준 표시 | Docker 실제 코드 실행·자원 제한 검증 → R0의 독립 보완 항목 |
| 안정성 | 중복 요청 방지, 임대 잠금, 백업·해시 검증·기존 DB 복원 | URL 확보 후 중단 복구, 복원 폴더 보호, 빈 경로 복원 → R0.1; 장애·진단 정리 → R7 |
| 기존 사진 이슈 | P1 사진 1장 최종 표시 미확인 이력 | 기존 글 재확인만 수행, 새 글 중복 생성 없이 기록 → R0 |
| 배포·공유 | 개발 환경에서 로컬 웹앱 실행 | 간편 설치·업데이트·내보내기, 이후 웹+로컬 실행기 검토 → R7 |

### 비공개 업로드만 제공하는 이유

의도된 제한이다. 1차 계획 D08·초기 발행 방식에 따라 원고·사진·카테고리·중복 방지부터 검증했다. 공개 발행은 원래 P5에 예정되어 있었으며 현재 UI·작업 모델·티스토리 어댑터가 비공개 기준으로 구현되어 있다.

2차에서는 공개/비공개 선택을 추가한다. 공개 발행, 무인 자동 발행, 예약 발행은 각각 별도 단계다. 첫 공개 기능은 사용자가 선택한 원고 버전과 발행 대상·공개 범위를 검토한 후 실행하는 방식으로 제공한다.

## 3. 외부 연동 조사와 결정 전제

아래는 2026-09-11 R1에서 기록한 조사·실험 결과다. 이번 로드맵 개정에서 외부 정책을 재조사한 것은 아니다. 연동 구현을 재개하거나 배포할 때 공식 경로·정책·실제 편집기 호환성을 다시 확인한다.

### 네이버 블로그

현재 공식 API 목록에는 블로그 검색과 공유 기능이 안내되어 있으나, 이 프로젝트가 필요한 본문·사진·카테고리의 완전한 글쓰기 API는 확인하지 못했다. 공유 기능을 자동 글쓰기 API로 간주하지 않는다. Playwright 실험에서는 별도 로그인, 문단·사진·캡션 교차 입력, 대표 사진, `강좌` 카테고리, 비공개 저장·재열기와 비로그인 접근 차단에 성공했다. [네이버 공식 API 목록](https://developers.naver.com/docs/common/openapiguide/apilist.md)

판정은 **조건부 지원 가능**이다. SmartEditor가 별도 도구를 지원 대상으로 안내하지 않으므로 UI 변경과 CAPTCHA에 취약하고 서비스가 보장하는 연동은 아니다. 추가 인증은 사용자에게 인계하고, 선택자·업로드·저장 결과가 불명확하면 중복 발행 없이 멈춘다. 상세 증거와 제한은 [R1 검증 보고서](docs/R1_INTEGRATION_REPORT.md)에 기록했다.

### Gemini와 웹 로그인

공식 Gemini CLI `0.59.0`에서 Google 로그인을 완료했지만, 개인용 클라이언트 지원 종료와 Antigravity CLI 이전 오류로 생성 단계에 도달하지 못했다. Google은 소비자용 Gemini CLI를 2026-06-18에 종료했다고 공지했다. [전환 공지](https://developers.googleblog.com/an-important-update-transitioning-gemini-cli-to-antigravity-cli/)

공식 Antigravity CLI `1.2.1`의 바이너리와 체크섬, 로그인 전 약관 화면, headless JSON 기능을 확인했다. 그러나 소비자 약관은 제3자 소프트웨어·도구를 통한 서비스 접근과 비Google 제품과의 결합을 제한한다. 이 앱에 소비자 로그인을 연결하는 방식은 **보류**한다. [Antigravity headless 문서](https://antigravity.google/docs/cli/headless/) · [추가 약관](https://antigravity.google/terms)

OAuth 토큰 추출·재사용은 계속 제외한다. FAQ가 안내하는 제3자 통합 경로는 API 키 또는 Vertex AI이므로 사용자가 별도 비용을 선택하기 전에는 구현하지 않는다. [공식 FAQ](https://geminicli.com/docs/resources/faq/)

추가 결제 없는 대안은 프롬프트·선택 사진 내보내기와 사용자가 공식 서비스에서 만든 결과의 수동 가져오기다. Gemini 웹페이지 자동 조작이나 유료 API를 자동 대체 경로로 채택하지 않는다. 상세 결과는 [R1 검증 보고서](docs/R1_INTEGRATION_REPORT.md)에 기록했다.

## 4. 개발 순서

| 순서 | 단계 | 사용자에게 제공할 결과 | 선행 조건·상태 |
| --- | --- | --- | --- |
| 완료 | R0·R1 | 백업 기반, 네이버 조건부 지원·Gemini 보류 판정 | 완료 이력 유지. 잔여 검증은 별도 추적 |
| 완료 | R0.1 복구 보강 | 중단된 작업 재확인, 안전한 복원, 새 설치 데이터 가져오기 | 구현·로컬 회귀 완료. 외부 계정 검증은 R7.2까지 추적 |
| 2 | R7.1 배포 기반 | 실행 의존성·최초 설정·자동 검사·설치 방식 확정 | R0.1 이후. 이후 단계에서 계속 유지 |
| 3 | R2 사진 중심 편집 | 본문 사이 사진, 메모·캡션·설명 분리, 기존 원고 변환 | R0.1, R1 관찰 결과 |
| 4 | R3 글 유형 확장 | 여행·정보·자유 글 생성과 블록 단위 편집 | R2 |
| 5 | R5 네이버 제품화 | 앱에서 네이버 연결·비공개 전송, 대상별 결과 추적 | R2, R1 재확인. 여행 원고 인수에는 R3 |
| 6 | R6 공개 발행 | 새 공개 글 작성과 기존 비공개 글 공개 전환 | 해당 플랫폼의 비공개 전송 검증 |
| 7 | R7.2 출시 검증 | 다른 PC에서 설치·업데이트·복원·발행 가능한 배포판 | 출시할 기능의 완료, R7.1 |
| 독립 선택 | R4 Gemini 대안 | 프롬프트/사진 내보내기·결과 가져오기 | R2. 유료 공급자 도입은 별도 비용 선택 시 |
| 독립 보완 | PS 실행 검증 | Python 예제와 실행 제한의 실측 결과 | Docker 환경 준비. 일반 글 출시의 선행 조건 아님 |
| 출시 후 | R7.3 예약·외부 접근 | 예약 정책, 웹 UI와 PC 실행기 연결 검토 | 로컬 운영 안정화 후 별도 설계 |

기본 순서는 **복구 보강 → 배포 기반 → 사진 편집 → 글 유형 확장 → 네이버 → 공개 발행 → 출시 검증**이다. 기존 확장 요구를 유지하면서 데이터 변경 전에 복구를 보강하고, 설치 문제를 마지막에 발견하지 않도록 R7 일부를 앞당겼다. R4는 본 흐름을 막지 않는다. 네이버가 지연되면 티스토리의 R6와 출시 검증을 먼저 진행할 수 있다.

고정 출시일이나 전체 완료율은 정하지 않는다. 각 단계 종료 때 실제 변경 범위·검증 결과·외부 연동 실패 원인을 기록하고 다음 단계 작업량을 다시 산정한다. 단계 착수와 완료는 아래 기준으로 판단한다.

## 5. 단계별 작업과 완료 기준

### R0. 기존 기능 정리

- [x] 저장·재편집·AI 적용 충돌·비공개 전송의 현재 동작을 회귀 검사 기준으로 고정한다.
- [x] DB·원고·이미지·문체를 일관된 시점으로 백업하고 복원 가능한지 확인한다. 실행 중 SQLite 파일 하나만 복사하는 방식은 피한다.
- [x] 브라우저 로그인 만료, 저장 직후 중단, 코드 검증 중단을 분리해 추적한다. 복구와 취소가 실제 외부 작업 상태를 과장하지 않도록 한다.
- [ ] P1 사진 표시 이력을 기존 글 재확인으로 정리한다.
- [ ] Docker 준비 후 PS 예제·경계 사례·시간/메모리/출력 제한·중단 복구를 검증한다. 환경 미준비 시 독립 대기 항목으로 남긴다.

완료 기준: 원고 구조를 변경하기 전 복구 가능한 백업과 현재 동작의 검증 기준을 확보한다. 외부 환경 때문에 남는 항목은 차단 원인과 재개 방법을 기록한다.

현재 결과: R0 핵심 완료. 해시로 검증된 실제 백업 1개와 자동 복원 검사를 확보했다. P1 사진 재확인과 Docker 실실행은 외부 상태가 필요한 독립 보완 항목으로 남는다.

### R0.1. 배포 전 복구·데이터 보호 보강

기존 R0의 완료를 취소하는 단계가 아니라 2026-09-22 점검에서 발견한 구체적인 결함을 수정하는 단계다.

- [x] 저장 URL을 확보한 작업이 중단 후 `unknown`이 되어도 기존 글만 읽어 재확인할 수 있게 한다. URL의 대상 블로그·게시글 경로와 원고 대응을 검증한다.
- [x] URL이 없는 결과 미확정 작업은 관리 화면의 기존 글 확인·연결 또는 미발행 확인 절차를 제공한다. 불명확한 상태에서 새 글을 자동 생성하지 않는다.
- [x] 복원 보존·실패·임시 폴더의 사진까지 Git 제외를 보장하고 실제 사용자 파일 없이 제외 규칙을 검사한다.
- [x] 새 데이터 경로 가져오기와 기존 DB 교체 복원을 구분한다. 복원 실패 시 원본·기존 데이터가 보존되게 한다.
- [x] 저장 전·저장 URL 확보 후·결과 조회 중 중단, 빈 경로 복원, 손상 백업 거절을 합성 자료로 재현하고 회귀 검사에 반영한다.

주요 작업 위치: `src/lib/store.ts`, `src/app/page.tsx`, `scripts/worker.ts`, `src/lib/backup.ts`, `scripts/backup.ts`, `.gitignore`, 관련 테스트와 복구 안내.

완료 기준: 이번 점검에서 재현한 두 오류와 Git 제외 누락이 해결되고, 중단 복구가 새 발행 작업을 만들지 않는다. 빈 경로·기존 데이터 복원이 모두 통과하며 기존 15개 검사·타입·빌드를 유지한다. 외부 글 결과는 실제 재확인 전까지 성공으로 바꾸지 않는다.

2026-09-22 결과: 단위 22개·타입·빌드, 격리된 복구 UI와 모의 티스토리 읽기 전용 검증 통과. 미발행 종료는 사용자 판단으로 기록하며 자동 재전송하지 않는다. 실제 티스토리 글 재확인·신규 발행과 사용자 데이터 교체는 수행하지 않았고 기존 확인 대기 1건은 유지한다.

### R1. 연동 가능성 검증

**네이버 실험**

- [x] 사용자가 지정한 네이버 블로그에서 직접 로그인하고 티스토리와 별도 프로필을 사용한다. 기존 하위 카테고리 `강좌`를 사용했다.
- [x] 문단 → 사진 → 사진 설명 → 문단 → 사진 → 사진 설명 → 문단 순서의 작은 원고를 입력한다.
- [x] 카테고리·대표 이미지·비공개 설정을 확인하고 테스트 글 1개를 저장한다. 임시저장과 비공개 게시를 구분했다.
- [x] 저장된 동일 글을 다시 열어 사진 순서·본문·공개 범위와 비로그인 접근을 검사한다.
- [x] 추가 인증·에디터 변경·업로드 실패·저장 결과 불명확 상태에서 자동 진행을 멈추는 경계를 구현하고, 저장 직후 결과 미확정에서 중복 발행 없이 기존 결과를 복구했다.

**Gemini 실험**

- [x] Gemini CLI `0.59.0`과 후속 Antigravity CLI `1.2.1`의 공식 실행 경로·버전·연동 범위를 확인했다.
- [ ] 브라우저 Google 로그인 → 텍스트 결과 수신. 로그인 후 기존 개인용 CLI 지원 종료 오류가 반환되어 **보류**.
- [ ] 사진 1장과 메모, 여러 사진 ID 대응. 약관상 제3자 앱 연동을 채택할 수 없어 실행하지 않음.
- [ ] 원고 구조 출력·부분 재작성·문체 분석 계약. 같은 이유로 실행하지 않음.
- [x] 유료 API·Vertex AI로의 자동 전환을 차단하고, 인증·취소·시간 초과·한도 처리는 정식 경로 채택 전까지 미지원으로 명시했다.

완료 기준: 두 실험을 각각 성공/조건부 지원/보류로 판정한다. 실패한 연동은 R2·R3를 막지 않으며, 성공하기 전 정식 지원으로 안내하지 않는다.

현재 결과: **네이버는 조건부 지원 가능, Gemini 소비자 로그인 연동은 보류**로 판정했다. R1은 판정 기준으로 완료했으며, 생성까지 실행하지 못한 Gemini 항목은 성공 체크하지 않았다. R2·R3는 진행 가능하다.

### R2. 사진 중심 편집과 공통 구조

블록 원고·편집·미리보기·전송 어댑터와 로컬 검증 완료. 2026-09-23 실제 티스토리 10장 비공개 전송·재열기·비로그인 차단도 통과했다. 기존 ‘Markdown 본문 + 끝에 붙는 사진 목록’을 다음과 같이 확장했다.

```text
도입 문단
소제목: 첫째 날
사진 A
짧은 사진 캡션
사진 A에 관한 긴 설명 문단
사진 B
여행 메모를 풀어 쓴 문단
정보 표 / 출처 / 마무리
```

- [x] Markdown 원문을 그대로 보존하는 본문 블록(문단·소제목·인용·목록·코드·표)과 사진 블록을 순서대로 저장한다. 사진은 파일 경로가 아닌 고유 이미지 ID를 참조한다.
- [x] 사진의 **작성용 메모**, **게시할 짧은 캡션**, **사진 옆에 배치할 긴 본문 설명**을 구분한다. 작성 메모가 자동 게시되지 않게 한다.
- [x] 드래그와 위/아래 버튼으로 순서를 바꾸고 사진 교체·제외·대표 이미지 선택을 지원한다.
- [x] 원본과 썸네일을 나눠 다량 업로드·진행률·실패 항목 재시도를 구현한다. 우선 50장을 성능 검증 목표로 삼고 플랫폼 제한·PC 메모리 결과에 맞춰 실제 상한을 정한다.
- [x] 여러 장 AI 분석은 선택 사진·장소/날짜별 묶음으로 나누고 이미지 해시·모델·메모 변경에 맞춰 분석을 재사용한다. 업로드 장수와 한 번의 AI 요청 장수는 분리한다.
- [x] 기존 원고는 본문 블록 + 기존 순서의 사진 블록으로 변환한다. 이전 버전은 보존하고 변환 실패 시 원본을 유지한다.
- [x] 블록을 원본으로 정하고 Markdown/HTML은 파생 표현으로 만든다. Markdown 수정이 필요한 경우 손실 없는 왕복 변환을 보장할 수 있는 범위부터 지원한다.
- [x] 블로그 연결에 플랫폼·계정/블로그 ID·프로필·카테고리 정보를 둔다. AI 연결에 공급자·모델·이미지 지원·연결/한도 상태를 분리한다.
- [x] 플랫폼마다 사진·표·코드·공개 범위 지원을 표시한다. 지원하지 않는 서식은 미리 알리고 원고를 조용히 훼손하지 않는다.

- [x] **실제 인수:** 사진 10장 교차 원고의 티스토리 비공개 저장·재열기·비로그인 차단. 사진 검토 안내로 한 차례 대기한 뒤 동일 글 재확인에서 통과. 중복 발행 없음.

로컬 실측: 단위 29개, 합성 PNG 50장 업로드/실패 1건 재시도/저장, 10장 교차 미리보기, 드래그·위아래 버튼·사진 교체, 모바일 390px 검사 통과. 새 AI 블록 출력은 R3 합성 3유형 실제 생성에서 검증했다. 선택 사진 분석 캐시는 로컬 계약 검사이며 실제 AI 검증은 별도다. 세부 사용법: [R2 사진 편집](docs/R2_PHOTO_EDITING.md).

완료 기준: 기존 원고와 버전을 유지하면서 사진 10장이 문단 사이에 배치된 원고의 저장·재열기·티스토리 전송을 확인한다. 50장 목표 자료로 UI 성능·실패 재시도를 따로 검증한다.

구현은 세 묶음으로 나눈다. **R2a 데이터·변환:** 스키마 버전, 안정적인 블록 ID, 기존 원고·과거 버전 보존, 플랫폼/AI 연결 및 대상별 발행 기록 계약을 정한다. **R2b 편집·미리보기:** 메모 분리, 순서 변경, 사진 교체와 썸네일을 제공한다. **R2c 전송·성능:** 티스토리 렌더러를 블록 순서에 맞추고 10장 실제 전송과 50장 로컬 성능을 검증한다. 기존 원고의 코드·표·캡션·대표 사진과 AI 적용 충돌 방지 기능도 유지해야 한다.

변환 전 `npm run audit`와 검증 가능한 백업을 확보한다. 새 스키마를 읽지 못하는 이전 앱으로 단순 교체하지 않도록 지원 버전을 기록하고, 되돌릴 때는 해당 버전과 호환되는 백업을 사용한다. 데이터 구조 구현은 주로 `src/lib/`, 생성 계약은 `src/services/ai/`, 전송은 `src/services/tistory/`에서 수행한다.

### R3. 여행·정보·자유 글과 문체 확장

| 유형 | 주요 입력 | 생성 결과에서 확인할 것 |
| --- | --- | --- |
| 여행 | 장소·날짜·일정, 사진별 메모, 실제 경험·비용 | 날짜/장소별 구성, 사진과 설명의 대응, 사용자가 말하지 않은 체험을 만들지 않음 |
| 정보 | 주제·독자·범위, 참고 자료, 확인 시점 | 설명·비교·활용법, 주장과 출처의 연결, 가격·영업시간 등 변경 가능 정보의 확인 수준 |
| 자유 | 하고 싶은 이야기, 메모·사진, 원하는 분위기 | 불필요한 기술/PS 항목 없이 글 생성, 사진 중심 또는 글 중심 구성 |

- [x] 글 유형에 맞는 입력란만 보여주고 기존 프로젝트·기술·PS는 유지한다.
- [x] 전체 초안 전에 개요와 사진 배치를 검토·수정할 수 있게 한다.
- [x] 선택 사진 설명만 생성하거나 특정 블록만 재작성한다. 잠근 문단과 사용자가 수정한 메모는 보존한다.
- [x] 사진만으로 실제 방문일·가격·가게 이름·사용자 감정을 확정하지 않는다. 모르는 내용은 보완 항목으로 남긴다.
- [x] 정보 글은 출처 접근과 사실 대조를 구분한다. URL 가져오기 범위는 출처 보관 규칙과 외부 요청 검증을 유지하며 확장한다.
- [x] 여행·정보용 대표 글과 문체를 추가하고 블로그별 기본 문체 및 글별 선택을 지원한다. 기술 글 3편으로 분석한 현재 문체를 모든 유형의 정답으로 취급하지 않는다.

원래 완료 기준: 실제 사용자 메모로 여행·정보·자유 글 각 1편을 작성하고 사진 설명 수정·순서 변경·부분 재작성·티스토리 비공개 전송까지 확인한다. 기존 프로젝트·기술·PS 원고도 저장·생성·재편집 회귀 검사를 통과해야 한다. 자료가 없으면 합성 검사까지만 기록하고 실제 사용자 자료 검증은 미완료로 남긴다.

2026-09-23 결과: 사용자가 **합성 자료로 먼저 검증**을 선택했다. 여행·정보·자유 각 1편의 실제 ChatGPT 생성→사진 설명/순서 수동 수정→ID 블록 재작성→티스토리 비공개 저장→세션 재시작 후 같은 글 재확인과 비로그인 차단을 통과했다. 잠금·메모·이전 버전 보존, 여섯 유형 생성/재편집 회귀, 문체 기본값/백업 검증도 통과했다. 개요 준비는 로컬 템플릿이며 AI 호출이 아니다. 상세: [R3 안내](docs/R3_WRITING.md).

- [x] 사용자가 선택한 합성 자료의 유형별 실제 생성·편집·비공개 전송 인수.
- [ ] 실제 사용자 메모·원본 사진·새 유형 문체 표본의 품질 인수. 이번 요청의 합성 검사와 구분하며 완료로 표시하지 않는다.

### R4. Gemini 대안 재평가

첫 범위는 수동 가져오기다. 원고 버전·이미지 ID를 포함한 요청 묶음을 내보내고, 결과를 별도 후보로 보관한 뒤 형식·이미지 참조·원고 버전 충돌을 확인해 검토 적용한다. 공식 API 채택 전에는 이를 자동 Gemini 연결이나 두 번째 공급자 로그인 지원으로 표시하지 않는다.

- [ ] 소비자 Antigravity 로그인을 앱에 연결하지 않는다. 수동 결과 가져오기 또는 사용자가 명시적으로 선택한 Gemini API/Vertex AI만 재평가한다.
- [ ] 생성 작업에 공급자·모델·원고/문체 버전·선택 자료를 고정한다. 연결 변경이 진행 중 작업의 공급자를 바꾸지 않게 한다.
- [ ] 초안 생성·선택 블록 재작성·사진 설명·문체 분석을 기능별 검증한다. 미지원 기능은 비활성화하고 이유를 표시한다.
- [ ] 같은 원고 입력으로 두 공급자의 결과를 비교하되 다른 공급자로의 자동 재생성을 기본 동작으로 두지 않는다.
- [ ] 계정 재로그인·로그아웃·사용량 미제공·한도 소진·취소 후 늦은 응답을 처리한다.

완료 기준: 수동 경로에서는 내보내기→사용자 생성→가져오기→검토 적용과 잘못된 형식·누락 이미지·오래된 원고 버전 거절을 확인한다. 유료 API 경로를 별도로 선택한 경우에만 공급자 로그인/인증·사용량·취소와 사진 포함 생성까지 검증한다. 두 경로 모두 ChatGPT 기능의 회귀 검사를 통과한다.

### R5. 네이버 정식 연결과 대상별 발행

- [ ] 블로그 연결 화면에서 티스토리·네이버를 별도 등록하고 카테고리·문체 기본값을 분리한다.
- [ ] R1에서 확인한 입력 방식으로 블록을 네이버 에디터에 옮긴다. 사진별 게시 ID와 블록 ID의 대응을 저장한다.
- [ ] 사진·본문·캡션·대표 이미지·카테고리·비공개 저장·결과 재확인을 구현한다.
- [ ] 네이버 대표 글은 가능한 공개 원문 가져오기 또는 붙여넣기로 문체 분석에 사용한다. 네이버 글을 티스토리 URL 검사에 억지로 통과시키지 않는다.
- [ ] 먼저 한 대상씩 전송하고 이후 원고 1개에서 두 대상 선택을 제공한다. 대상마다 별도 발행 기록·글 ID·URL·원고 버전·공개 범위를 보관한다.
- [ ] 한 플랫폼만 성공하면 성공한 글은 유지하고 실패 대상만 복구한다. 플랫폼·블로그·원고 버전·동작을 함께 식별해 중복을 막는다.

완료 기준: 사진과 문단이 교차하는 여행 원고를 네이버에 비공개 저장·재열기하고, 같은 원고의 티스토리 결과와 각각 추적할 수 있다. 두 플랫폼에서 모양이 완전히 같다고 보장하지 않는다.

제품화 순서는 연결·카테고리 → 공통 작업 실행기/이력 → 블록 입력 → 결과 검증·중단 복구다. `src/services/naver/probe.ts`의 고정 실험 원고를 그대로 제품 발행 경로로 사용하지 않는다. CAPTCHA·로그인 만료·UI 변경 시 사용자에게 필요한 조치를 보여주고, 한 대상 성공/다른 대상 실패 시 성공한 글이 재생성되지 않는지 확인한다.

### R6. 공개 발행과 기존 글 공개 전환

- [ ] 공개/비공개를 원고가 아닌 대상별 발행 설정에 저장한다. 플랫폼별 추가 공개 범위는 실제 검증 후 노출한다.
- [ ] 최종 화면에 플랫폼·블로그·카테고리·원고 버전·대표 이미지·공개 범위를 함께 표시한다. 기본값은 비공개로 유지한다.
- [ ] 새 글 발행과 기존 비공개 글의 공개 전환을 별도 동작으로 만든다. 기존 글 ID가 있으면 공개 전환 때문에 새 글을 만들지 않는다.
- [ ] 공개 전환 전 현재 글과 연결된 원고 버전을 확인한다. 원격에서 수정된 글을 자동 덮어쓰지 않는다.
- [ ] 저장 결과를 알 수 없으면 같은 글을 조회해 상태를 확인한다. 공개 글을 만들거나 삭제하는 재시도를 자동 수행하지 않는다.
- [ ] 사용자가 지정한 검증용 원고로 공개 설정·비로그인 표시를 확인한다. 실제 공개 검증의 대상과 실행 시점은 그 단계에서 정한다.

완료 기준: 새 공개 발행 및 기존 비공개 글의 공개 전환을 구분하고 글 수가 불필요하게 늘어나지 않음을 확인한다. 공개 기능 완료가 무인 발행·예약 지원을 뜻하지 않는다.

### R7. 운영 안정화와 공유

#### R7.1. 초반에 갖출 배포 기반

- [x] `tsx`를 실행 의존성에 포함하고 npm ci/build/prune 후 TypeScript 없는 설치에서 웹앱·작업 실행기의 동시 시작·종료 검증.
- [x] 개발자 블로그 기본값 제거, 최초 설정 UI, Node·Chrome·AI 실행 파일·포트·폴더 쓰기 권한 진단. AI 파일이 없어도 수동 저장 가능함을 확인.
- [x] Windows `Install.cmd`/`Start.cmd`/`Stop.cmd`/`Doctor.cmd`, 데이터 위치와 오류 확인 안내. 중복 실행·작업 실행기 오류·다른 포트 소유자 보존 검사.
- [x] Windows CI에 타입·단위·빌드·개인 자료 추적 제외·새 설치 API 검사를 정의. 로컬 동등 명령 통과, GitHub 원격 실행은 미실행. 외부 계정 검사는 기본 검사에서 제외.
- [x] Node 22.20 이상인 22.x, 잠금 파일 의존성·시스템 Chrome·기존 AI 검증 버전, UNLICENSED 상태와 외부 배포 전 라이선스/고지 확정 기준, 설치 문서·CHANGELOG 마련.

실측 제한: Windows Node 22.20에서 한글 경로의 CommonJS 판별 오류를 재현했다. 이번 지원 설치 경로는 ASCII 문자(공백 허용)로 한정하고 설치 전 진단으로 차단한다. 비ASCII 경로 지원은 R7.2 호환성 항목이다. 설치 안내: [Windows 로컬 설치](docs/WINDOWS_SETUP.md).

완료 기준: 깨끗한 설치 환경에서 문서대로 시작·종료하고 초기 설정·수동 원고 저장이 가능하다. 배포 설치가 개발 PC의 전역 패키지·개인 블로그 설정에 암묵적으로 의존하지 않는다. 이 단계만으로 외부 계정 검증이나 베타 출시를 완료 처리하지 않는다.

#### R7.2. 출시할 기능의 안정화와 타 PC 인수

- [ ] 발행 전/사진 업로드 중/저장 직후/결과 조회 중 중단, 네트워크 끊김·로그인 만료를 재현하고 적절한 재로그인·재확인·재시도 경로를 제공한다.
- [ ] AI 취소·시간 초과 후 늦은 결과가 적용되지 않고 관련 프로세스가 종료되는지 확인한다. PS 실행을 포함하면 컨테이너 중단·정리도 실측한다.
- [ ] 기존 전체 백업·복원을 UI/런처에서 사용할 수 있게 한다. 선택 내보내기를 추가할 때 참조 사진·문체 버전이 빠지지 않도록 검증한다. 인증은 공유·백업에서 제외하고 재로그인한다.
- [ ] 업데이트 전 백업·마이그레이션·실패 시 되돌리기, 데이터와 인증을 보존하는 앱 업데이트, 사용자가 선택하는 데이터 삭제 절차를 검증한다.
- [ ] 진단 자료의 민감 정보 마스킹·보관·삭제 기준을 적용하고 원문/인증 값 없이 오류를 전달할 방법을 제공한다.
- [ ] 다른 Windows PC와 다른 계정에서 설치→로그인→원고 복원→생성→편집→비공개 전송→재열기까지 수행한다. 공개 기능을 포함하면 해당 플랫폼의 R6 검사도 수행한다.
- [ ] 기존 P1 사진 이력을 같은 글에서 재확인한다. 미해결이면 원인과 지원 제한을 기록하고 해당 사진 표시 시나리오를 검증 완료로 주장하지 않는다.

완료 기준: 배포 대상 PC에서 출시 범위의 전체 흐름이 통과하고, 데이터 손실·중복 발행·복구 불가 결함이 해결된다. 남은 비차단 이슈와 지원 제한을 공개하며, 설치/업데이트/복원 증거와 지원 버전을 남긴다.

#### R7.3. 출시 후 선택 확장

- [ ] 예약 발행은 플랫폼 자체 예약 가능성을 먼저 확인한다. 로컬 예약이면 시간대·절전·꺼짐·지난 예약·중복 실행·취소 정책을 정한다.
- [ ] 외부 웹 UI + 사용자 PC 실행기 페어링을 검토한다. 인증·연결 해제·권한·재연결·대기 작업 동기화를 별도 설계한다.
- [ ] 서버형 다중 사용자 서비스는 사용자 인증·권한, 데이터·브라우저 세션 격리, 인증 저장, 장기 작업 실행, 운영 비용을 별도 계획으로 만든다.

R7.3은 첫 로컬 배포의 필수 조건이 아니다. 로컬 앱 배포에 도메인은 필수가 아니며, 외부 웹 배포는 별도 후속 선택이다.

## 6. 당장 시작할 다음 작업

R0.1·R7.1 구현/로컬 검사, R2 사진 10장 실제 전송, R3 구현 및 합성 실제 인수까지 완료했다.

1. R5: 네이버 연결·카테고리를 제품 UI와 공통 대기열에 연결한다.
2. 플랫폼별 사진/문단 전송·대표·비공개와 개별 이력/글 ID·중단 복구를 검증한다.
3. R6 공개 전환과 R7.2 타 PC·설치/업데이트/복원 인수를 진행한다.
4. 실제 사용자 여행·정보·자유 자료와 문체 품질, 기존 P1 사진, 선택 사진 분석 캐시의 외부 AI 검사는 별도 미완료로 추적한다.

우선순위 밖: 대량 무인 발행, 결제·사용자별 과금, 서버형 다중 사용자 운영, 영상 편집, LaTeX 전체 지원, 모든 언어의 PS 실행 환경. 필요해지면 후속 로드맵에서 별도로 정한다.

## 7. 출시 단위와 통과 기준

아래는 배포 가능한 지점을 나눈 것이다. 실제 출시는 각 인수 기준이 충족된 뒤 판단하며, 모든 단계가 끝나야만 시험 사용을 시작할 필요는 없다.

| 출시 단위 | 제공 범위 | 필수 완료 조건 | 다음 출시로 남기는 기능 |
| --- | --- | --- | --- |
| 로컬 비공개 베타 | 기존 3개 글 유형·ChatGPT·티스토리 비공개 저장 | R0.1·R7.1, 해당 범위의 R7.2 타 PC·복구 인수 | 사진 블록·새 글 유형·네이버·공개 발행 |
| 사진·일반 글 베타 | 위 기능 + 본문 중간 사진·여행/정보/자유 글 | R2·R3, 변환/복원 및 해당 범위 R7.2 재검증 | 네이버·공개 발행 |
| 다중 플랫폼 출시 후보 | 위 기능 + 네이버 비공개 전송·대상별 이력 | R5, 플랫폼별 중단·부분 성공 복구와 R7.2 | 검증하지 않은 플랫폼의 공개 발행 |
| 로컬 1차 배포판 | 확장 글 유형·티스토리/네이버·사용자 확인 후 공개/비공개 발행 | 출시 대상으로 선언한 각 플랫폼의 R6, 전체 R7.2, 설치/업데이트/롤백 검증 | Gemini 자동 연결·예약·서버형 서비스 |

네이버가 막히면 티스토리만 지원하는 배포판을 먼저 낼 수 있으며, 이때 지원 플랫폼을 명확히 줄여 표시한다. 이는 네이버 계획의 완료나 폐기를 뜻하지 않는다. R4는 검증된 수동 가져오기 기능부터 별도 추가할 수 있다.

## 8. 검증·일정 운영 원칙

- 코드 변경마다 관련 단위 검사·타입·빌드를 수행한다. 원고/에디터 변경은 로컬 UI·모바일 표시를 추가 확인한다.
- 데이터 구조 변경마다 변경 전 백업, 변환 후 무결성·이미지 참조·과거 버전, 실패/되돌리기를 검증한다.
- 외부 연동은 계정·플랫폼·검사 날짜·원고 버전·결과를 기록한다. 로컬 테스트 통과나 예전 게시 성공을 현재 외부 검증으로 대신하지 않는다.
- 플랫폼별 비공개 검증 후 공개 검증으로 이동한다. 새 글과 기존 글 공개 전환을 구분하며 실제 공개 시험 대상은 R6 착수 때 지정한다.
- 로그인·CAPTCHA·실제 사용자 자료·다른 PC·Docker 준비가 필요한 작업은 의존 조건을 기록하고, 무관한 기능 개발은 계속 진행한다.
- 단계 종료 시 완료 항목·실패/미실행 항목·다음 작업을 `PROJECT_PLAN.md`에 기록한다. 일정 추정은 해당 단계 작업 분해 후 갱신하고 외부 검증 대기는 구분한다.
