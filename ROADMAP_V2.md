# 글담 개발 로드맵 2차

작성일: 2026-09-11 · 상태: 개발 제안 및 착수 순서 정리, 신규 기능 구현 전

## 1. 제품 방향

**사진과 메모, 참고 자료를 모아 내 문체의 글을 만들고 티스토리·네이버 블로그에 게시하는 개인용 로컬 웹앱.**

기존 프로젝트·기술·PS 글을 유지하면서 여행·정보·자유 글을 추가한다. AI는 ChatGPT를 유지하고 Gemini 계정 로그인 연동을 검증한다. 원고를 만든 AI와 발행할 블로그는 서로 독립적으로 선택한다.

이번 문서는 1차 계획의 미완료 P5·P6와 새로운 확장 요청을 재배치한다. 실제 구현·검증 이력의 기준은 [PROJECT_PLAN.md](PROJECT_PLAN.md)이며, 이 문서의 체크박스는 앞으로 할 일이다. 계획 수립만으로 공개 발행, 새 계정 로그인, 유료 서비스 결제·설치가 실행되는 것은 아니다.

## 2. 현재 가능한 것과 남은 것

| 영역 | 현재 구현·검증 | 남은 작업과 2차 배치 |
| --- | --- | --- |
| 티스토리 | 개인 블로그 1개, 비공개 저장·재열기·코드·표 확인 | 여러 사진의 중간 배치, 공개 설정, 다른 스킨 대응 → R2·R6 |
| 원고·사진 | 버전 저장, 폴더 가져오기, 이미지당 캡션, 대표 이미지, 최대 20장·본문 끝 배치 | 사진 사이 긴 설명, 순서 편집, 다량 사진 성능 → R2 |
| AI | ChatGPT 로그인, 초안·부분 재작성, 한도·취소·결과 검토 | 공급자별 상태 분리, Gemini → R1·R4 |
| 문체 | 원문 분석·규칙 수정·버전·생성 적용 | 여행·정보 유형, 블로그별 문체, 네이버 원문 가져오기 → R3·R5 |
| 기술·PS | 참고 자료, 해설·예제·검증 수준 표시 | Docker 실제 코드 실행·자원 제한 검증 → R0의 독립 보완 항목 |
| 안정성 | 중복 요청 방지, 임대 잠금, 미확정 저장 결과 재확인 | 중단 지점별 복구 검증, 백업·복원, 진단 정리 → R0·R7 |
| 기존 사진 이슈 | P1 사진 1장 최종 표시 미확인 이력 | 기존 글 재확인만 수행, 새 글 중복 생성 없이 기록 → R0 |
| 배포·공유 | 개발 환경에서 로컬 웹앱 실행 | 간편 설치·업데이트·내보내기, 이후 웹+로컬 실행기 검토 → R7 |

### 비공개 업로드만 제공하는 이유

의도된 제한이다. 1차 계획 D08·초기 발행 방식에 따라 원고·사진·카테고리·중복 방지부터 검증했다. 공개 발행은 원래 P5에 예정되어 있었으며 현재 UI·작업 모델·티스토리 어댑터가 비공개 기준으로 구현되어 있다.

2차에서는 공개/비공개 선택을 추가한다. 공개 발행, 무인 자동 발행, 예약 발행은 각각 별도 단계다. 첫 공개 기능은 사용자가 선택한 원고 버전과 발행 대상·공개 범위를 검토한 후 실행하는 방식으로 제공한다.

## 3. 외부 연동 조사와 결정 전제

### 네이버 블로그

현재 공식 API 목록에는 블로그 검색과 공유 기능이 안내되어 있으나, 이 프로젝트가 필요한 본문·사진·카테고리의 완전한 글쓰기 API는 확인하지 못했다. 공유 기능을 자동 글쓰기 API로 간주하지 않는다. **따라서 Playwright 기반 전용 어댑터를 검증 후보로 삼는다.** API 목록만으로 자동화 허용 여부나 실제 성공 가능성을 확정할 수는 없다. [네이버 공식 API 목록](https://developers.naver.com/docs/common/openapiguide/apilist.md)

검증 대상은 직접 로그인, 에디터 진입, 문단·사진 교차 입력, 카테고리·대표 이미지·비공개 저장·재열기다. 티스토리의 HTML 입력 방식을 그대로 사용할 수 있다고 가정하지 않는다. 브라우저 화면과 서비스 운영 조건을 확인한 뒤 지원 범위를 정한다. 추가 인증은 사용자에게 인계하며, 자동화가 불안정하면 원고·사진 패키지 내보내기와 수동 붙여넣기를 제공한다.

### Gemini와 웹 로그인

공식 Gemini CLI는 브라우저에서 Google 계정으로 로그인하고 인증을 로컬에 보관하는 방식을 안내한다. 기존 인증이 있으면 headless 실행에서도 사용한다. 이를 근거로 **공식 CLI 자체를 실행해 결과를 받는 로컬 연동**을 1차 검증 후보로 정한다. 아직 이 앱에서 로그인·생성을 성공시킨 것은 아니다. [공식 인증 문서](https://geminicli.com/docs/get-started/authentication/)

CLI의 headless JSON 출력은 응답과 사용량을 프로그램에서 읽는 데 사용할 수 있다. 다만 응답을 JSON으로 감싼다는 사실과 우리 원고 블록 스키마를 정확히 생성한다는 보장은 다르므로 별도 결과 검증이 필요하다. [공식 headless 문서](https://geminicli.com/docs/cli/headless/)

Google은 CLI의 OAuth 인증을 다른 도구가 가져다 백엔드에 직접 접근하는 방식을 제한한다. 인증 토큰 추출·재사용 방식은 계획에서 제외한다. 공식 CLI 호출을 앱에 연결하는 구체적인 사용 범위도 착수 시 확인하며, 이 문서만으로 배포 가능한 연동이라고 확정하지 않는다. [공식 FAQ](https://geminicli.com/docs/resources/faq/)

Google 로그인 방식의 무료 사용 구간과 구독별 한도가 안내되어 있다. Gemini 웹앱의 대화·기능·한도가 그대로 연동된다고 가정하지 않는다. 계정 종류·지역·모델 가용성과 실제 한도를 확인하고, 별도 API 결제나 크레딧 사용으로 자동 전환하지 않는다. [공식 사용량 안내](https://geminicli.com/docs/resources/quota-and-pricing/)

공식 CLI 경로가 적합하지 않으면 우선 프롬프트·선택 사진 내보내기와 결과 가져오기를 제공한다. Gemini 웹페이지 자동 조작이나 유료 API를 검증 없이 대체 경로로 채택하지 않는다. 사진 분석·문체 분석·취소 지원은 각각 검증 후 기능을 활성화한다.

## 4. 개발 순서

| 단계 | 목표 | 주요 결과 | 의존성 |
| --- | --- | --- | --- |
| R0 | 기존 기능 정리·데이터 보호 | 미해결 이슈 목록, 마이그레이션 전 백업, 핵심 회귀 검사 | 지금 시작 가능 |
| R1 | 네이버·Gemini 가능성 검증 | 각각의 작은 실험과 지원/제한/대안 결정 | 현재 구조에서 독립 실험 |
| R2 | 사진 중심 원고·공통 연결 구조 | 본문 중간 사진, 사진별 설명, 플랫폼·AI 식별자 | R0, R1의 입력/출력 관찰 |
| R3 | 여행·정보·자유 글 | 유형별 자료 입력·구성·문체·사진 배치 생성 | R2 |
| R4 | Gemini 정식 선택 기능 | 생성·재작성·문체 분석에 공급자 선택 | R1 Gemini 통과, R2 |
| R5 | 네이버 정식 발행 기능 | 별도 로그인, 카테고리, 블록 전송·검증 | R1 네이버 통과, R2; 여행 검증에 R3 |
| R6 | 공개 발행 | 대상별 공개 범위·발행/전환·결과 확인 | 해당 플랫폼의 비공개 전송 검증 |
| R7 | 일상 사용·배포 | 복구·백업·간편 실행·예약·공유 | 핵심 작성·발행 흐름 안정화 |

권장 순서는 **작은 연동 실험 → 사진 편집 기반 → 여행/정보 글 → Gemini·네이버 정식 연결 → 공개 발행 → 운영 편의**다. R4와 R5는 서로의 완료를 기다릴 필요가 없으며, 네이버가 지연되면 티스토리 공개 기능을 먼저 제공할 수 있다. Docker 설치는 여행·네이버·Gemini 개발의 선행 조건으로 두지 않는다. 일정은 R1 결과를 보고 단계별로 산정한다.

## 5. 단계별 작업과 완료 기준

### R0. 기존 기능 정리

- [ ] 저장·재편집·AI 적용 충돌·비공개 전송의 현재 동작을 회귀 검사 기준으로 고정한다.
- [ ] DB·원고·이미지·문체를 일관된 시점으로 백업하고 복원 가능한지 확인한다. 실행 중 SQLite 파일 하나만 복사하는 방식은 피한다.
- [ ] 브라우저 로그인 만료, 저장 직후 중단, 코드 검증 중단을 분리해 추적한다. 복구와 취소가 실제 외부 작업 상태를 과장하지 않도록 한다.
- [ ] P1 사진 표시 이력을 기존 글 재확인으로 정리한다.
- [ ] Docker 준비 후 PS 예제·경계 사례·시간/메모리/출력 제한·중단 복구를 검증한다. 환경 미준비 시 독립 대기 항목으로 남긴다.

완료 기준: 원고 구조를 변경하기 전 복구 가능한 백업과 현재 동작의 검증 기준을 확보한다. 외부 환경 때문에 남는 항목은 차단 원인과 재개 방법을 기록한다.

### R1. 연동 가능성 검증

**네이버 실험**

- [ ] 사용자가 지정한 네이버 블로그와 테스트 카테고리에서 직접 로그인한다. 티스토리와 별도 프로필을 사용한다.
- [ ] 문단 → 사진 → 사진 설명 → 문단 → 사진 순서의 작은 원고를 입력한다.
- [ ] 카테고리·대표 이미지·비공개 설정을 확인하고 테스트 글 1개를 저장한다. 임시저장과 비공개 게시를 구분한다.
- [ ] 저장된 동일 글을 다시 열어 사진 순서·본문·공개 범위와 비로그인 접근을 검사한다.
- [ ] 추가 인증·에디터 변경·업로드 실패·저장 결과 불명확 상태에서 자동 진행을 멈추는 경계를 확인한다.

**Gemini 실험**

- [ ] 공식 실행 경로와 연동 범위를 확인하고 버전을 고정한다.
- [ ] 브라우저 Google 로그인 → 공식 CLI 실행 → 텍스트 결과 수신을 확인한다.
- [ ] 사진 1장과 메모로 설명 생성, 사진 여러 장의 ID 대응을 확인한다. 필요한 파일 접근은 선택 자료로만 한정한다.
- [ ] 원고 구조 출력, 부분 재작성, 문체 분석의 각 응답을 기존 결과 계약에 맞출 수 있는지 확인한다.
- [ ] 인증 만료·취소·시간 초과·한도 소진의 표시를 검증하고 유료 경로 자동 전환을 차단한다.

완료 기준: 두 실험을 각각 성공/조건부 지원/보류로 판정한다. 실패한 연동은 R2·R3를 막지 않으며, 성공하기 전 정식 지원으로 안내하지 않는다.

### R2. 사진 중심 편집과 공통 구조

가장 먼저 체감 품질을 높일 본 구현이다. 현재의 ‘Markdown 본문 + 끝에 붙는 사진 목록’을 다음과 같이 확장한다.

```text
도입 문단
소제목: 첫째 날
사진 A
짧은 사진 캡션
사진 A에 관한 긴 설명 문단
사진 B
여행 메모를 풀어 쓴 문단
정보 표 / 출처 / 마무리
```

- [ ] 문단·소제목·사진·인용·목록·코드·표를 순서가 있는 블록으로 저장한다. 사진은 파일 경로가 아닌 고유 이미지 ID를 참조한다.
- [ ] 사진의 **작성용 메모**, **게시할 짧은 캡션**, **사진 옆에 배치할 긴 본문 설명**을 구분한다. 작성 메모가 자동 게시되지 않게 한다.
- [ ] 드래그와 위/아래 버튼으로 순서를 바꾸고 사진 교체·제외·대표 이미지 선택을 지원한다.
- [ ] 원본과 썸네일을 나눠 다량 업로드·진행률·실패 항목 재시도를 구현한다. 우선 50장을 성능 검증 목표로 삼고 플랫폼 제한·PC 메모리 결과에 맞춰 실제 상한을 정한다.
- [ ] 여러 장 AI 분석은 선택 사진·장소/날짜별 묶음으로 나누고 이미지 해시·모델·메모 변경에 맞춰 분석을 재사용한다. 업로드 장수와 한 번의 AI 요청 장수는 분리한다.
- [ ] 기존 원고는 본문 블록 + 기존 순서의 사진 블록으로 변환한다. 이전 버전은 보존하고 변환 실패 시 원본을 유지한다.
- [ ] 블록을 원본으로 정하고 Markdown/HTML은 파생 표현으로 만든다. Markdown 수정이 필요한 경우 손실 없는 왕복 변환을 보장할 수 있는 범위부터 지원한다.
- [ ] 블로그 연결에 플랫폼·계정/블로그 ID·프로필·카테고리 정보를 둔다. AI 연결에 공급자·모델·이미지 지원·연결/한도 상태를 분리한다.
- [ ] 플랫폼마다 사진·표·코드·공개 범위 지원을 표시한다. 지원하지 않는 서식은 미리 알리고 원고를 조용히 훼손하지 않는다.

완료 기준: 기존 원고와 버전을 유지하면서 사진 10장이 문단 사이에 배치된 원고의 저장·재열기·티스토리 전송을 확인한다. 50장 목표 자료로 UI 성능·실패 재시도를 따로 검증한다.

### R3. 여행·정보·자유 글과 문체 확장

| 유형 | 주요 입력 | 생성 결과에서 확인할 것 |
| --- | --- | --- |
| 여행 | 장소·날짜·일정, 사진별 메모, 실제 경험·비용 | 날짜/장소별 구성, 사진과 설명의 대응, 사용자가 말하지 않은 체험을 만들지 않음 |
| 정보 | 주제·독자·범위, 참고 자료, 확인 시점 | 설명·비교·활용법, 주장과 출처의 연결, 가격·영업시간 등 변경 가능 정보의 확인 수준 |
| 자유 | 하고 싶은 이야기, 메모·사진, 원하는 분위기 | 불필요한 기술/PS 항목 없이 글 생성, 사진 중심 또는 글 중심 구성 |

- [ ] 글 유형에 맞는 입력란만 보여주고 기존 프로젝트·기술·PS는 유지한다.
- [ ] 전체 초안 전에 개요와 사진 배치를 검토·수정할 수 있게 한다.
- [ ] 선택 사진 설명만 생성하거나 특정 블록만 재작성한다. 잠근 문단과 사용자가 수정한 메모는 보존한다.
- [ ] 사진만으로 실제 방문일·가격·가게 이름·사용자 감정을 확정하지 않는다. 모르는 내용은 보완 항목으로 남긴다.
- [ ] 정보 글은 출처 접근과 사실 대조를 구분한다. URL 가져오기 범위는 출처 보관 규칙과 외부 요청 검증을 유지하며 확장한다.
- [ ] 여행·정보용 대표 글과 문체를 추가하고 블로그별 기본 문체 및 글별 선택을 지원한다. 기술 글 3편으로 분석한 현재 문체를 모든 유형의 정답으로 취급하지 않는다.

완료 기준: 실제 사용자 메모로 여행 글·정보 글 각 1편을 작성하고 사진 설명 수정·순서 변경·부분 재작성·비공개 전송까지 확인한다.

### R4. Gemini 정식 연결

- [ ] R1에서 통과한 공식 경로만 공급자 어댑터로 연결한다. ChatGPT와 Gemini의 인증·한도·오류·모델 목록을 분리한다.
- [ ] 생성 작업에 공급자·모델·원고/문체 버전·선택 자료를 고정한다. 연결 변경이 진행 중 작업의 공급자를 바꾸지 않게 한다.
- [ ] 초안 생성·선택 블록 재작성·사진 설명·문체 분석을 기능별 검증한다. 미지원 기능은 비활성화하고 이유를 표시한다.
- [ ] 같은 원고 입력으로 두 공급자의 결과를 비교하되 다른 공급자로의 자동 재생성을 기본 동작으로 두지 않는다.
- [ ] 계정 재로그인·로그아웃·사용량 미제공·한도 소진·취소 후 늦은 응답을 처리한다.

완료 기준: Google 계정 로그인 방식으로 사진 포함 원고 생성과 결과 적용을 확인하고 기존 ChatGPT 기능의 회귀 검사를 통과한다. 공식 경로로 충족되지 않으면 수동 결과 가져오기 수준을 명확히 표시한다.

### R5. 네이버 정식 연결과 대상별 발행

- [ ] 블로그 연결 화면에서 티스토리·네이버를 별도 등록하고 카테고리·문체 기본값을 분리한다.
- [ ] R1에서 확인한 입력 방식으로 블록을 네이버 에디터에 옮긴다. 사진별 게시 ID와 블록 ID의 대응을 저장한다.
- [ ] 사진·본문·캡션·대표 이미지·카테고리·비공개 저장·결과 재확인을 구현한다.
- [ ] 네이버 대표 글은 가능한 공개 원문 가져오기 또는 붙여넣기로 문체 분석에 사용한다. 네이버 글을 티스토리 URL 검사에 억지로 통과시키지 않는다.
- [ ] 먼저 한 대상씩 전송하고 이후 원고 1개에서 두 대상 선택을 제공한다. 대상마다 별도 발행 기록·글 ID·URL·원고 버전·공개 범위를 보관한다.
- [ ] 한 플랫폼만 성공하면 성공한 글은 유지하고 실패 대상만 복구한다. 플랫폼·블로그·원고 버전·동작을 함께 식별해 중복을 막는다.

완료 기준: 사진과 문단이 교차하는 여행 원고를 네이버에 비공개 저장·재열기하고, 같은 원고의 티스토리 결과와 각각 추적할 수 있다. 두 플랫폼에서 모양이 완전히 같다고 보장하지 않는다.

### R6. 공개 발행과 기존 글 공개 전환

- [ ] 공개/비공개를 원고가 아닌 대상별 발행 설정에 저장한다. 플랫폼별 추가 공개 범위는 실제 검증 후 노출한다.
- [ ] 최종 화면에 플랫폼·블로그·카테고리·원고 버전·대표 이미지·공개 범위를 함께 표시한다. 기본값은 비공개로 유지한다.
- [ ] 새 글 발행과 기존 비공개 글의 공개 전환을 별도 동작으로 만든다. 기존 글 ID가 있으면 공개 전환 때문에 새 글을 만들지 않는다.
- [ ] 공개 전환 전 현재 글과 연결된 원고 버전을 확인한다. 원격에서 수정된 글을 자동 덮어쓰지 않는다.
- [ ] 저장 결과를 알 수 없으면 같은 글을 조회해 상태를 확인한다. 공개 글을 만들거나 삭제하는 재시도를 자동 수행하지 않는다.
- [ ] 사용자가 지정한 검증용 원고로 공개 설정·비로그인 표시를 확인한다. 실제 공개 검증의 대상과 실행 시점은 그 단계에서 정한다.

완료 기준: 새 공개 발행 및 기존 비공개 글의 공개 전환을 구분하고 글 수가 불필요하게 늘어나지 않음을 확인한다. 공개 기능 완료가 무인 발행·예약 지원을 뜻하지 않는다.

### R7. 운영 안정화와 공유

- [ ] 발행 전/사진 업로드 중/저장 직후/결과 조회 중의 중단을 재현하고 사용자에게 재로그인·재확인·재시도의 적절한 다음 행동을 보여준다.
- [ ] 중단/취소된 AI·코드 실행 프로세스의 종료와 재시작 복구를 확인한다. 진단 자료에 원문·인증 값이 불필요하게 남지 않도록 정리한다.
- [ ] 원고·사진·문체·설정의 선택 백업·복원과 버전 마이그레이션을 제공한다. 인증은 일반 공유·백업에서 제외하고 재로그인한다.
- [ ] Windows에서 간단히 실행·종료·업데이트할 수 있는 설치/런처를 제공한다. 다른 사람이 자기 계정으로 설치하는 방법을 문서화한다.
- [ ] 예약은 별도 기능으로 설계한다. 플랫폼 자체 예약 여부를 먼저 확인하고, 로컬 예약은 PC 절전·꺼짐·시간대·지연 실행 정책을 명시한다.
- [ ] 이후 외부 웹 UI + 사용자 PC 실행기 페어링을 검토한다. 여러 사람이 쓰는 서버형 서비스는 계정 격리·인증 저장·운영 비용을 별도 설계한다.

완료 기준: 다른 PC에서 자기 계정으로 설치·로그인·원고 복원·생성·전송이 가능하고, 오류가 나도 저장 결과와 다음 행동을 알 수 있다. 로컬 앱 공유에는 도메인이 필수는 아니다. 외부 웹 배포는 별도 후속 선택이다.

## 6. 당장 시작할 다음 작업

1. **R0:** 마이그레이션용 백업·회귀 검사 기준을 정리한다. Docker는 별도 대기로 유지할 수 있다.
2. **R1:** 네이버의 작은 사진 글과 Gemini의 텍스트/사진 생성으로 연동 방법을 먼저 확정한다. 필요한 계정·블로그 주소는 실험 착수 시 받는다.
3. **R2:** 사진·메모·캡션·본문 문단의 데이터와 편집 화면을 구현한다. 이 단계가 여행 글과 두 플랫폼 전송의 공통 기반이다.

우선순위 밖: 대량 무인 발행, 결제·사용자별 과금, 서버형 다중 사용자 운영, 영상 편집, LaTeX 전체 지원, 모든 언어의 PS 실행 환경. 필요해지면 후속 로드맵에서 별도로 정한다.
