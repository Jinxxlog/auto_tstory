# 티스토리 글 작성·발행 도구: 구현 계획 및 작업 기록

최종 갱신: 2026-09-23 (Asia/Seoul)

이 문서는 구현 계획과 진행 상태의 단일 기준이다. 작업 시작 전에 읽고, 종료 후 실제 변경·검증·남은 작업을 갱신한다. 아직 구현하지 않은 항목은 계획이며 제품의 현재 기능을 뜻하지 않는다.

2차 확장 계획과 착수 순서: [ROADMAP_V2.md](ROADMAP_V2.md) (2026-09-22 개정). 아래 P0~P4 검증 이력은 유지한다. R0.1 복구 보강·R7.1 배포 기반은 구현 및 로컬 검증 완료이며, 이후 R2·R3·R5·R6 → R7.2 출시 검증 순서로 진행한다. R4 및 R7.3은 선택 확장이다. R2 사진 10장 실제 비공개 전송 인수를 완료했다. R3 유형 확장 구현과 사용자가 선택한 합성 자료의 실제 AI 생성·편집·비공개 전송 인수도 완료했다. 실제 사용자 자료·문체 품질 검증은 별도로 남기며 다음 제품 단계는 R5다.

## 1. 현재 상태

- 2026-09-23 공개·학습 자료화 점검: 실제 호출 경로/데이터 모델/보안·성능·운영을 분석하고 [기술 보고서](docs/TECHNICAL_ANALYSIS_2026-09-23.md), [학습 가이드](docs/LEARNING_GUIDE.md), [공개 검사](docs/PUBLICATION_AUDIT_2026-09-23.md)를 작성. 기존 32개 테스트·타입·빌드·빌드 추적 9개 검사·3유형 합성 UI 통과. 사진 분석 apply의 비원자적 두 저장을 합성 오류 주입으로 재현했으며 **수정하지 않음**. 기능 수정은 사용자의 별도 요청 후 진행. 기존 R5 제품 순서는 유지한다.

- 2026-09-22 현황 점검: 개인용 티스토리 비공개 작성 MVP 단계이며 일반 배포 전 복구·설치 검증 필요. 단위 15개·타입·프로덕션 빌드·로컬 브라우저 검사 통과, DB 무결성 정상과 재확인 작업 1건 유지. 저장 URL 확보 후 중단 복구 차단, 빈 경로 복원 실패, 복원 폴더 이미지의 Git 제외 누락 확인. 상세: [배포 준비 점검](docs/DEPLOYMENT_READINESS_2026-09-22.md). 외부 AI/블로그 실동작은 이번에 재검증하지 않음.
- 전체 상태: P0~P4 핵심 흐름 완료. 2차 R0 백업·복원과 회귀 기준 확보 완료. R1 연동 가능성 검증 완료: 네이버는 조건부 지원 가능, Gemini 소비자 계정 로그인 연동은 정책상 보류. Docker 실실행과 P1 사진 재확인은 독립 대기.
- 완료: 원고 편집·저장·재편집, 사진/폴더 가져오기·표지 라이브러리, 블로그 연결·카테고리, SQLite 작업 대기열·잠금·중복 방지, 비공개 전송 및 저장 결과 재확인.
- P0 검증 결과: 게시글 ID `15`, 카테고리 `테스트`, 비공개. 본문·사진 2장·대표 이미지 보존, 비로그인 접근 차단 및 재시작 후 재열기 확인.
- P1 검증 결과: 웹앱 원고로 새 비공개 글 1개 저장. 관리 목록의 카테고리·비공개 표시, 재편집 화면의 제목·본문·사진 순서·캡션, 게시글의 코드·표·첫 사진·대표 이미지, 비로그인 접근 차단 확인. 두 번째 사진은 티스토리 검토 안내로 표시되어 작업을 확인 대기로 유지.
- P2 검증 결과: ChatGPT Plus 로그인·사진 입력 모델 목록 확인, 요약과 합성 사진으로 초안·캡션 생성, 부분 재작성, 원고 v2/v3 적용 및 수동 편집 후 v4 저장. 생성한 글의 비공개 저장 성공; 최종 재확인 결과는 아래 작업 기록 참조.
- P3 구현: 대표 글 URL/붙여넣기·본문 정제·유형 관리, 문체 분석·규칙 편집·버전 확정, 생성 요청에 규칙/예시 스냅샷 반영, 기본/사용자 문체 비교 화면. 실제 공개 기술 글 3편 분석과 문체 v2 저장 확인.
- R1 검증 결과: 네이버 전용 Chrome에서 문단·사진·캡션 교차 입력, 첫 사진 대표 설정, `강좌` 카테고리, 비공개 게시 1개, 저장 후 재열기와 비로그인 차단을 확인. 저장 직후 결과 미확정에서도 재발행 없이 기존 글을 복구했다. Gemini CLI 로그인은 개인용 지원 종료 오류로 생성하지 못했고, 후속 Antigravity 소비자 약관상 제3자 앱 결합 제한을 확인해 제품 연동을 보류했다.
- 범위와 한계: 티스토리와 네이버 모두 단일 계정·현재 편집기에서만 검증. 네이버 자동화는 공식 지원 연동이 아니며 UI 변경·CAPTCHA에 취약하다. 장기 무인 운영, 다른 스킨/계정, 예약·공개 발행은 미검증/미구현. Docker 미설치로 PS 실제 코드 실행은 미검증. 현재 문체 분석 표본은 기술 글 3편으로 전체 문체를 대표한다고 단정하지 않음.
- R0.1 결과: 중단 작업의 읽기 전용 재확인, URL 없는 작업의 후보 글 연결/사용자 미발행 확인, 복원 폴더 Git 제외, 빈 경로 복원 구현·로컬 회귀 검증 완료. 실제 외부 글 검사는 이번에 수행하지 않았으며 기존 확인 대기 작업 1건 유지.
- R7.1 결과: 실행 의존성·최초 블로그 설정·환경 진단·Windows 설치/실행/종료·CI 정의·안내 문서 구현. 격리된 새 소스 설치에서 개발 의존성 제거 후 웹앱/실행기 가동·원고 저장·재시작·종료·오류 정리 검증 완료. CI의 GitHub 원격 실행과 다른 PC 인수는 미실행. 현재 지원 설치 경로는 ASCII 문자(공백 허용)이며 한글 경로는 Node 모듈 판별 오류를 재현해 사전 차단한다.
- R2 현황: 본문/사진 블록·메모/캡션/설명 분리·50장 로컬 편집 구현 및 회귀 통과. 2026-09-23 재로그인 후 합성 사진 10장 비공개 전송·재열기·코드/표·사진 식별자/순서/크기·대표 이미지·비로그인 차단 통과. 티스토리 사진 검토 안내가 끝난 뒤 같은 글을 재확인했으며 중복 발행하지 않음.
- R3 현황: 여행·정보·자유 입력, 개요/사진 배치 검토, 블록 ID 재작성·AI 잠금, 유형별 출처/문체·블로그 기본값 구현. 단위 32개와 UI 회귀 통과. 사용자 선택에 따라 합성 자료로 각 1편 실제 ChatGPT 생성→수동 편집→블록 재작성→티스토리 비공개 전송·재열기·비로그인 차단 완료. 실제 사용자 메모·원본 사진·새 문체 표본의 품질은 미검증. 상세: [R3 사용/검증 안내](docs/R3_WRITING.md).
- 다음 작업: R5 네이버 제품화 → R6 공개 발행 → R7.2 출시 검증. 실제 사용자 자료 품질 인수와 기존 P1 사진 재확인, 선택 사진 분석 캐시의 외부 AI 검사, 한글 설치 경로·업데이트/롤백은 별도로 추적한다. R4 Gemini 대안·Docker 실제 실행·R7.3 예약/외부 접근은 독립 조건이다.
- 현재 범위: 개인 사용자 1명, 블로그 1개, 내 PC에서 실행하는 웹앱.
- 초기 발행 방식: 사용자가 원고를 확인한 뒤 비공개 저장. 공개 자동 발행은 후속 단계에서 설정으로 제공.
- Git 상태: 공개 저장소 `https://github.com/Jinxxlog/auto_tstory` 생성 및 초기 커밋 push 완료. 로컬 `main`은 `origin/main`을 추적한다.

## 2. 목표와 범위

사용자는 글 유형, 요약 또는 문제/기술 자료, 사진, 카테고리를 입력한다. 앱은 기존 블로그의 문체와 구조를 활용해 원고를 만들고, 편집 가능한 미리보기를 제공한 뒤 티스토리 에디터로 전송한다.

지원할 글 유형:

| 유형 | 필수 입력 | 선택 입력 | 결과 구조 |
| --- | --- | --- | --- |
| 프로젝트 | 요약, 실제 구현 내용 | 기술 스택, 사진, 어려웠던 점, 회고 | 배경, 구현, 화면 설명, 문제 해결, 회고 |
| 기술 | 주제와 설명 범위 | 사용자 해석, 참고 URL/본문, 독자 수준 | 개념, 원리, 예시, 비교, 주의점, 출처 |
| 여행 | 장소·요약 | 날짜·일정·직접 경험·비용·사진 메모 | 개요/사진 배치 검토 후 경험 기반 구성 |
| 정보 | 주제·독자·범위·요약 | 참고 자료·확인 시점 | 설명·비교·활용·출처/보완 질문 |
| 자유 | 이야기·메모 | 사진·분위기 | 사진 또는 글 중심 구성 |
| PS | 문제 본문 또는 접근 가능한 URL, 제약 조건, 언어 | 본인 코드, 접근 과정 | 문제 요약, 접근법, 정당성, 복잡도, 코드, 예제 |

초기 범위에서 제외하는 항목: 다중 사용자 SaaS, 결제, 대량 발행, 무인 로그인 보장, 여러 블로그 동시 처리, 모델 파인튜닝. 이들은 사용자가 요청하면 재평가한다.

## 3. 핵심 결정과 전제

| ID | 결정 | 이유 / 영향 |
| --- | --- | --- |
| D01 | 브라우저 자동화는 Playwright로 시작 | 입력·파일 업로드·인증 상태를 같은 실행 환경에서 관리 |
| D02 | 전용 프로필의 표시되는 브라우저에서 직접 로그인 | 비밀번호 저장을 피하고 추가 인증을 사용자가 처리 가능 |
| D03 | 로컬 웹앱을 먼저 구현 | 사용자 PC의 사진과 브라우저에 접근하기 쉬움. 실행 중인 PC 필요 |
| D04 | 웹 UI와 자동화 작업 실행을 분리 | 긴 작업과 브라우저 오류가 화면 요청의 수명에 종속되지 않도록 함 |
| D05 | AI는 공식 API를 사용하는 공급자 교체 구조 | 일반 ChatGPT/Claude 로그인으로 API 사용 권한을 얻는다고 가정하지 않음 |
| D06 | 문체 규칙과 대표 글 예시를 저장해 재사용 | 초기에는 파인튜닝이나 벡터 DB 없이 시작 가능 |
| D07 | 원고는 구조화된 문서로 저장 | 이미지 위치·코드·표·출처를 유지하며 미리보기와 발행에 공통 사용 |
| D08 | P0에서 비공개 저장을 먼저 검증 | 업로드 가능성과 포맷 보존을 작은 범위에서 확인 |
| D09 | 내부 비공개 HTTP API를 핵심 의존성으로 삼지 않음 | 공식 지원되지 않는 요청 형식에 의존하는 범위를 줄임 |
| D10 | 최신 프로필 저장소는 유지하고 갱신된 쿠키를 별도로 저장·복원 | 프로필만으로 세션 쿠키가 유지되지 않는 사례 확인. 서버 측 만료는 수동 재로그인으로 처리 |
| D11 | HTML로 본문·이미지를 입력하고 기본모드에서 본문 이미지를 대표로 선택 | 실제 저장·재열기 검증 성공. 별도 대표 이미지 파일 업로드는 오류 원인 미확정으로 최종 흐름에서 제외 |
| D12 | P1은 Markdown을 정제한 HTML로 입력한 뒤 기본모드의 실제 사진 첨부 UI 사용 | 일반 원고의 이미지·캡션 보존을 위해 P0의 HTML 사진 입력 경로를 변경. 사진은 본문 아래에 배치 |
| D13 | 저장 URL을 확보하면 같은 작업에서 읽기 전용 결과 재확인 | 티스토리 사진 처리·검토 지연을 성공으로 오인하거나 새 글을 중복 생성하지 않음 |
| D14 | P1 원고는 Markdown과 별도 이미지 목록, 전송 작업에는 확정 원고 스냅샷 저장 | 편집·미리보기 구현 범위를 명확히 하고 이후 수정이 기존 작업 내용을 바꾸지 않도록 함. 블록별 이미지 배치는 후속 단계 |
| D15 | 사용자 요청으로 P2 첫 공급자를 ChatGPT 로그인 기반 Codex App Server로 변경 | D05의 첫 공급자 API 키 계획을 대체. 공식 stdio 프로토콜·앱 전용 인증 저장소 사용. API 키 인증과 환경 키 상속을 차단하며 구독 한도를 사용 |
| D16 | AI 결과는 별도 보관하고 사용자가 검토 후 버전 일치 시 적용 | 생성 중 수동 편집한 내용을 덮어쓰지 않음. 부분 재작성은 유일한 선택 구간만 교체. 적용 전후 원고 버전 보관 |
| D17 | 저장 결과 확인은 전용 읽기 탭 사용 | 티스토리 재편집 화면의 beforeunload가 이동을 막는 사례 확인. 원고를 버리는 확인을 자동 승인하지 않고 저장된 글을 검사 |
| D18 | 대표 글·분석 작업·확정 문체를 별도 저장하고 생성 요청에 버전/예시 고정 | 원문 수정/제외와 문체 규칙 변경이 이미 요청한 생성 결과의 기준을 바꾸지 않음. 새 초안과 사용자 수정본의 자동 학습 없음 |
| D19 | 공개 글 URL은 설정 블로그의 개별 글로 제한, 실패 시 붙여넣기 | 로그인 쿠키 없이 가져오며 외부 호스트·관리 경로로의 리디렉션을 거절. 본문 영역을 찾지 못하면 전체 페이지를 대신 분석하지 않음 |
| D20 | P4 자료·문제·코드·예제를 선택 입력 구조로 추가하고 생성 요청에 출처 원문 고정 | 기존 원고 호환 유지. 공식 도메인 접근과 붙여넣기 출처를 구분하고 접근 성공을 사실 검증으로 표시하지 않음 |
| D21 | PS 실행은 Python Docker 어댑터로 시작하며 환경이 없으면 미실행 | 호스트에서 사용자/AI 코드를 실행하지 않음. 네트워크·호스트 마운트 차단, 자원·시간·출력 제한. 현재 Docker 미설치로 실제 실행 검증 대기 |
| D22 | 두 번째 AI 공급자는 후속 선택 항목으로 보류 | 사용자의 별도 API 결제 불가·ChatGPT 사용 선택을 유지. 현재 공급자 계약으로 기술·PS 모두 생성 |
| D23 | P4 수식은 일반 텍스트·inline code로 한정 | 코드·표·O(N) 표시를 실제 티스토리에서 확인. LaTeX 변환은 구현하지 않음 |
| D24 | 사용자 요청에 따라 네이버·Gemini·여행/정보/자유 글을 2차 범위로 계획 | D22의 두 번째 공급자 보류를 Gemini 공식 로그인 경로 검증으로 변경. 현재 지원 완료를 뜻하지 않으며 별도 API 결제 자동 전환 없음 |
| D25 | 사진 중심 블록 원고와 대상별 발행 기록을 확장 공통 기반으로 제안 | 작성 메모·게시 캡션·긴 설명 분리, 기존 버전 보존, 플랫폼별 성공/실패·공개 범위와 글 ID 추적. 구현 전 백업·마이그레이션 검증 필요 |
| D26 | 공개 발행은 대상별 설정과 기존 글 공개 전환을 구분해 추가 계획 | 초기 비공개 제한은 의도된 설계. 2차에서 공개 기능을 구현하되 실제 공개 검증 대상은 착수 시 정하고 무인·예약 발행과 분리 |
| D27 | R0 백업은 SQLite 온라인 백업 API와 이미지 해시 manifest를 사용 | WAL 사용 중 파일 복사를 피하고 DB 스냅샷에 포함된 이미지 목록만 복사. 인증·진단 제외. 복원 전 안전 백업과 기존 데이터 폴더를 모두 보존 |
| D28 | 네이버는 Playwright 기반 조건부 지원 후보로 진행 | R1에서 사진·문단 교차 입력과 비공개 저장·재열기에 성공했지만 공식 글쓰기 API와 SmartEditor 도구 지원이 없음. UI 불일치·CAPTCHA·결과 미확정에서 멈추고 중복 발행을 금지 |
| D29 | Gemini 소비자 로그인 연동은 보류하고 수동 결과 가져오기를 우선 검토 | 기존 Gemini CLI 개인용 인증 경로가 종료됐고 후속 Antigravity 소비자 약관이 제3자 앱 결합을 제한. 유료 API/Vertex AI는 사용자가 비용을 선택하기 전 자동 도입하지 않음 |
| D30 | 2026-09-22 로드맵 개정에서 R0.1 복구와 R7.1 배포 기반을 확장 개발 앞에 배치 | 데이터 마이그레이션 전에 재확인/복원 결함을 해결하고 설치 문제를 조기에 발견. 기존 R2·R3·R5·R6 확장 범위 유지, R7.2 출시 인수와 R7.3 예약/외부 접근 분리. 단계별 베타는 선택 가능한 배포 지점이며 실제 출시 완료를 뜻하지 않음 |
| D31 | 복구 입력 URL은 검증 후보로 보관하고 읽기 전용 검사 성공 후 저장 결과로 확정 | 같은 작업 ID·원고 스냅샷·중복 키 유지. 제목·주소·본문·카테고리·비공개 및 사진 검사 실패 시 확인 대기. 미발행 종료는 사용자 판단을 기록하고 자동 재전송하지 않음. 새 설치 복원은 빈/없는 폴더만 허용하고 기존 데이터 교체에는 안전 복사 유지 |
| D32 | R7.1은 Node 22.20 이상인 22.x와 Windows 소스 폴더 설치를 지원 | npm ci로 빌드한 뒤 개발 의존성을 제거하고 tsx는 실행 의존성으로 유지. next.config.mjs로 실행 시 TypeScript 의존을 제거. 설치별 인증된 로컬 제어·소유한 자식 프로세스 종료 사용. Node 22.20에서 비ASCII 경로의 CommonJS 판별 오류를 최소 사례로 재현해 현재는 ASCII 설치 경로만 지원하고 설치 전에 안내. 자동 업데이트/롤백·다른 PC 인수와 구분 |
| D33 | 사용자 데이터·인증·백업은 배포 빌드 추적에서도 제외 | Next의 동적 경로 추적에 실제 데이터·백업 경로가 들어감을 발견해 outputFileTracingExcludes와 빌드 결과 검사 추가. 설치/CI에서 누락 여부를 검사. 배포 라이선스는 UNLICENSED로 명시하고 오픈소스 권한을 임의 부여하지 않음. 외부 배포 전 라이선스·제3자 고지 확정 필요 |


| D34 | R2는 기존 데이터를 일괄 덮어쓰지 않고 편집 후 저장 시 새 schemaVersion 2 원고를 생성 | 본문 Markdown과 사진을 안정적 ID의 블록으로 저장. 기존 draft_versions JSON 및 전송 스냅샷 불변. 본문 블록 내부 Markdown은 그대로 보존하고 전체 HTML↔Markdown 왕복 변환을 하지 않음. 최초 블록 저장은 migrations 6, 이전 앱으로 되돌릴 때는 R2 이전 백업 사용 |
| D35 | 로컬 원고 50장과 한 번의 AI 요청 10장을 구분 | 정규화 PNG 원본·파생 WebP 썸네일, 순차 업로드·진행률·개별 재시도. 50장 합성 로컬 검사는 실제 50장 티스토리 전송이나 고해상도 실사진 성능 보장이 아님. 실제 전송 인수는 10장 |
| D36 | 사진 작성 메모/묶음은 게시하지 않으며 AI 요청에서만 참고 | 선택 사진과 메모를 전송한다는 UI 안내. 이미지 해시·공급자·모델·메모·묶음·프롬프트 버전으로 캐시, 한 번에 최대 10장. 검토 적용·버전 충돌·취소 후 늦은 결과 차단. 블로그 대상과 AI 공급자 연결 계약 분리 |
| D37 | 티스토리 네이티브 사진 요소를 지정 본문 위치로 이동하고 전체 순서를 검증 | 저장 전 문단 사이 사진 위치 및 제목/카테고리/비공개/대표 이미지 확인. 업로드 사진 경로를 작업에 고정해 재확인에서도 식별자·순서 대조. 실제 호환성 검증이 끝나기 전 R2 전체 완료로 기록하지 않음 |

| D38 | R3 개요는 로컬 구성안과 사용자의 명시적 검토로 시작 | 빈 원고에 유형별 기본 구조를 제안하고 기존 본문은 보존. 자료·블록·개요의 정규화 토큰으로 검토 신선도를 확인하며 새 유형 전체 생성 전에 검토/저장 요구. 개요용 별도 AI 호출은 하지 않음 |
| D39 | 블록 재작성은 문자열 탐색 대신 안정적 ID를 사용하고 AI 잠금을 강제 | 동일 문장·빈 설명·사진 50장에서도 한 블록만 변경. 대상 사진만 전송, 캡션·메모·비선택 블록 보존. 기존 선택 구간 재작성도 유지하고 저장 결과 재검증 시 중복 교체/안내 삽입 방지 |
| D40 | 문체 기본값을 블로그·유형별로 분리하고 원고에서 재선택 가능 | 기술 전용 문체가 여행/정보/자유에 묵시적으로 적용되지 않음. 기본값도 백업하며 작업에는 당시 문체 버전 고정. 사용자는 이번 인수를 합성 자료로 선택했으므로 실제 사용자 품질 인수와 구분 |

결정에는 초기 설계와 검증 후 변경이 함께 포함된다. 후속 결정이 이전 단계의 구현 방식을 변경할 수 있으며 이유와 영향을 기록한다.

공개·분석 결정 D41 (2026-09-23): 사용자는 공개 업로드와 기술/학습 자료화를 요청했고 기능 결함은 먼저 진단한 뒤 별도 수정 요청을 받도록 지정했다. 따라서 이번에는 README·환경변수 예제·Git 제외·보고서만 변경하고 기존 미커밋 구현은 그대로 검사해 공개한다. 라이선스 신규 선택·기록 재작성·실제 AI/블로그 재실행은 하지 않는다. .env.example은 설정 참고용이며 실행기/worker에 자동 로더를 추가하지 않는다.

## 4. 구성과 디렉터리 계획

기본 기술: TypeScript, Next.js, Node.js 작업 실행 프로세스, Playwright, SQLite, 로컬 파일 저장소. 정확한 버전과 DB 라이브러리는 초기화 시 공식 호환성 문서를 확인하고 고정한다.

```text
웹 UI → 로컬 API → SQLite 작업 대기열 → 작업 실행 프로세스
                         ├─ AI 공급자 API
                         ├─ 원고 / 문체 / 이미지 저장소
                         └─ Playwright → 전용 브라우저 → 티스토리
```

```text
src/app/                 화면과 로컬 API
src/domain/              원고·작업·문체 데이터 규칙
src/services/ai/         공급자별 API 연결
src/services/style/      기존 글 정제·문체 분석·예시 선택
src/services/content/    유형별 생성·검증·렌더링
src/services/tistory/    티스토리 로그인·카테고리·에디터·저장 결과 확인
src/services/naver/      네이버 R1 조건부 자동화 실험과 저장 결과 확인
src/worker/              작업 실행·복구·취소
scripts/                 P0 실행 도구와 개발 보조 명령
tests/                   핵심 흐름 검증
data/                    로컬 DB·원본·생성 원고·이미지 (Git 제외)
.local/                  브라우저 프로필·민감 진단 자료 (Git 제외)
PROJECT_PLAN.md          계획 및 진행 기록
AGENTS.md                작업 시 문서 읽기·갱신 지침
```

위 구조는 전체 계획이다. 실제 구현은 `src/app/`에 화면/API, `src/lib/`에 원고·SQLite·이미지·보안 규칙, `scripts/worker.ts`에 작업 실행기, `src/services/tistory/publisher.ts`에 전송 어댑터를 둔다. P2는 `src/services/ai/`에 공급자 계약·Codex 연결·생성/검증·실행기를 추가했다. AI 작업도 같은 실행 임대 아래 직렬 처리하며 별도 테이블에 보관한다. P3 문체 서비스는 `src/services/style/`에, P4 출처 및 코드 검증은 `src/services/references.ts`와 `src/services/code-check.ts`에 구현되어 있다. R1 네이버 실험은 `src/services/naver/`와 `scripts/r1-naver.ts`에 격리했으며 아직 웹 UI·공통 대기열에 연결한 R5 제품 기능은 아니다. Gemini CLI는 임시 검증 후 제거해 앱 의존성에 추가하지 않았다.

## 5. 사용자 흐름과 화면

1. 설정: 블로그 주소 입력 → 전용 브라우저 로그인 → 연결 확인 → 카테고리 읽기.
2. AI 연결: 원고 화면에서 ChatGPT 연결 → 공식 화면 로그인 → 계정의 사용 가능한 모델 선택. 별도 API 키는 사용하지 않음.
3. 문체 설정: 대표 글 선택 또는 본문 가져오기 → 분석 → 규칙 수정·확정.
4. 새 글: 프로젝트/기술/PS 선택 → 요약·자료·사진 입력 → 카테고리 지정.
5. 사진 정리: 기본 표지 또는 글별 사진 선택 → 순서·캡션·대표 이미지 조정.
6. 초안 생성: 입력 누락 확인 → 개요 → 본문·이미지 배치 → 검증 결과 표시.
7. 편집: 제목·본문·코드·이미지 위치 수정, 특정 구간 재작성, 미리보기.
8. 전송: 원고 버전 확정 → 티스토리 입력 → 비공개 저장 → 결과 URL 확인.
9. 작업 이력: 생성·전송 상태, 실패 단계, 재로그인, 복구, 저장된 글 열기.

화면은 설정, 문체 관리, 새 글/편집, 작업 이력의 네 영역으로 시작한다. 초기에는 원본 폴더 자동 감시보다 사용자가 선택한 폴더/파일 가져오기를 우선한다.

## 6. 데이터 설계

| 데이터 | 주요 필드 / 책임 |
| --- | --- |
| BlogConnection | 블로그 URL, 연결 상태, 전용 프로필 위치 참조, 마지막 확인 시각 |
| Category | 블로그, 실제 UI에서 식별한 카테고리 값 또는 경로, 표시 이름, 확인 시각 |
| SourcePost | 원문 URL, 제목, 글 유형, 정제 본문, 수집 시각, 사용자가 선택했는지 |
| StyleProfile | 유형, 버전, 문체 규칙, 대표 글 참조, 사용자 수정 사항 |
| Asset | 로컬 경로, 해시, MIME, 크기, 순서, 캡션, 대표 이미지 여부 |
| Draft | 유형, 사용자 입력, 출처, 현재 원고 버전, 카테고리 |
| DraftVersion | 제목, 구조화된 본문, 이미지 참조, 문체 버전, 생성/수정 구분 |
| Job | 종류, 상태, 실행 단계, 재시도 횟수, 잠금 정보, 오류, 관련 원고 버전 |
| Publication | 작업 ID, 발행 대상, 원고 버전, 공개 범위, 글 ID/URL, 확인 결과 |
| UsageRecord | 공급자, 모델, 반환된 사용량, 추정 비용, 관련 작업 |

본문 블록은 문단, 제목, 목록, 코드(언어 포함), 이미지(Asset 참조), 표, 인용을 기본으로 한다. 수식은 P0/P4에서 티스토리 표현 가능성을 확인한 뒤 지원 범위를 정한다.

API 키는 평문 DB에 저장하지 않는다. 초기에는 서버 환경변수로 시작하고, 설정 UI에서 저장하려면 OS 자격 증명 저장소 등 적절한 저장 방식을 먼저 구현한다. 원고 버전과 발행 결과는 연결해 이후 원고 수정이 이전 발행 기록을 바꾸지 않도록 한다.

## 7. 문체 분석과 글 생성 설계

### 문체 분석

- 대표 글 10~20개를 시작점으로 제안하되 적은 글로도 분석 가능하게 한다.
- 공개 글은 실제 본문 영역만 추출하고 메뉴·광고·댓글을 제거한다.
- RSS나 목록 하나만으로 전체 글 수집이 완료됐다고 가정하지 않는다. URL 직접 입력과 본문 붙여넣기를 대안으로 제공한다.
- 문장 끝맺음, 길이, 소제목 구성, 도입/마무리, 코드·표·사진 배치를 분석한다.
- 프로젝트·기술·PS 공통 규칙과 유형별 규칙을 구분하고 사용자가 수정할 수 있게 한다.
- 새 원고에 필요한 규칙과 대표 예시만 전달한다. 생성 초안은 자동으로 문체 기준에 편입하지 않는다.

### 공통 생성 순서

입력 정규화 → 부족한 사실 표시 → 개요 생성 → 본문 생성 → 이미지 위치 연결 → 구조 검증 → 문체 검토 → 편집 가능한 초안 저장.

- 공급자 인터페이스는 텍스트/이미지 입력, 구조화 결과, 사용량, 오류를 공통 형태로 반환한다.
- 모델이 이미지 입력을 지원하는지 확인하고, 지원하지 않으면 이미지 분석 기능을 제한하거나 다른 모델을 선택하도록 안내한다.
- 이미지 설명은 사용자가 고칠 수 있으며, 사진으로 확인되지 않는 구현 경험·수치·성과는 생성하지 않는다.
- 원문과 웹 자료는 참고 데이터로 취급하고, 자료 속 명령이 도구 실행이나 발행 설정을 변경하지 않도록 한다.
- 구조화 결과의 스키마 오류는 제한 횟수로 교정 요청하고 실패하면 초안 상태로 보존한다.
- 공급자가 사용량을 반환하지 않으면 알 수 없음으로 표시하며, 예상 비용을 실제 청구액으로 표시하지 않는다.

### 유형별 검증

- 프로젝트: 제공된 기능·기술 스택과 본문 일치 여부, 미제공 경험이나 수치 생성 여부.
- 기술: 핵심 주장과 공식 자료 연결, 사용자 해석과 검증된 사실 구분, 자료 확인 날짜 저장.
- PS: 문제 조건 누락, 알고리즘 정당성, 시간·공간 복잡도, 예제와 경계 사례 확인.
- PS 코드 실행은 별도 제한 환경을 준비한 뒤 도입한다. 실행 시간·메모리 제한과 네트워크 차단을 적용한다. 실행하지 않은 코드와 예제만 통과한 코드는 정답 검증 완료로 표시하지 않는다.
- 미리보기와 티스토리용 HTML 변환에는 허용된 요소만 사용해 스크립트 등 실행 콘텐츠를 제거한다.

## 8. 티스토리 자동화 설계

1. 전용 프로필로 브라우저를 열고 로그인 여부를 확인한다.
2. 필요한 경우 사용자가 직접 로그인한 후 작업을 재개한다.
3. 선택한 블로그와 카테고리가 실제 대상과 일치하는지 확인한다.
4. 편집 화면의 현재 모드·프레임·본문 입력 방법을 확인한다.
5. 사진을 실제 첨부 UI로 올리고, 첨부 완료와 원고 이미지의 대응 관계를 확인한다.
6. 제목·본문·사진 순서·코드·표·대표 이미지·카테고리를 설정한다.
7. 원고 버전과 공개 범위를 확인한 뒤 해당 저장 동작을 실행한다.
8. 저장 결과의 글 ID/URL을 기록하고 다시 열어 내용·카테고리·공개 범위를 확인한다.

에디터 선택자와 조작 방법은 별도 어댑터에 모은다. HTML/마크다운/기본 모드 중 어떤 방식이 안정적인지는 P0 결과로 결정한다. 로컬 파일 경로를 본문에 넣는 것만으로 이미지 게시가 완료됐다고 간주하지 않는다.

인증 화면이나 CAPTCHA가 나타나면 `needs_login` 또는 `needs_attention`으로 전환하고 사용자가 처리한 후 재개한다. 티스토리의 실제 자동화 수용 여부와 서비스 정책은 운영 전 확인할 항목이며, API 종료 공지는 자동화 허용이나 차단 여부를 증명하지 않는다.

## 9. 작업 상태와 중복 방지

기본 상태: `queued`, `running`, `needs_login`, `needs_attention`, `succeeded`, `failed`, `cancelled`, `unknown`.

- 실행 단계는 상태와 별도로 기록: 자료 분석, 생성, 브라우저 연결, 이미지 첨부, 본문 입력, 저장 요청, 결과 확인.
- 사용자 요청마다 고유 작업 ID를 부여하고 중복 클릭을 같은 요청으로 처리한다.
- 발행 요청에는 블로그·원고 버전·공개 범위를 고정한다. 같은 요청의 재전송은 기존 결과를 우선 반환한다.
- 저장 요청 전 오류는 복구 가능한 단계에서 재시도한다. AI 재시도 횟수와 비용에도 상한을 둔다.
- 저장 요청 후 응답 유실은 `unknown`으로 기록한다. 관리 목록과 저장된 결과를 대조하기 전에는 새 글 저장을 반복하지 않는다.
- 제목 하나만으로 같은 글이라고 판정하지 않는다. 확보한 글 ID/URL, 원고 특징, 저장 시각 등을 함께 확인하고 확신이 없으면 확인 대기로 남긴다.
- 작업 실행 프로세스가 중단되면 잠금 만료와 마지막 단계를 확인해 복구한다. 외부 저장의 정확히 한 번 실행은 보장한다고 표현하지 않는다.
- 취소는 안전한 단계에서 중단한다. 이미 저장한 글은 취소 요청만으로 자동 삭제하지 않는다.
- DB 상태 변경은 짧은 트랜잭션으로 처리하고 긴 AI/브라우저 작업 동안 DB 잠금을 유지하지 않는다.

## 10. 단계별 구현 계획

### P0. 자동화 가능성 검증 — 최우선

- [x] Node.js 등 로컬 실행 환경 확인, 최소 TypeScript/Playwright 프로젝트 초기화.
- [x] `.gitignore`에 프로필·키·DB·원본 자료·민감 진단 결과 제외 규칙 추가.
- [x] 전용 프로필로 표시되는 브라우저 실행, 수동 로그인 대기.
- [x] 브라우저 재시작 후 인증 재사용 여부 확인. (최종 쿠키 저장 방식으로 관리 화면 재접속 및 저장된 글 재열기 성공)
- [x] 실제 블로그와 카테고리 목록 읽기.
- [x] 고정 테스트 원고로 제목·문단·목록·코드·표 입력.
- [x] 이미지 2장 이상 첨부, 순서와 대표 이미지 확인.
- [x] 비공개 저장 후 다시 열어 서식·사진·카테고리·공개 범위 확인.
- [x] 세션 만료 또는 로그아웃 시 재로그인 대기 흐름 확인. (인증 없는 별도 컨텍스트의 로그인 이동 및 실제 수동 재로그인 확인; 서버 만료를 강제로 재현한 것은 아님)
- [x] 사용한 에디터 모드와 실패 지점, 재현 절차를 이 문서에 기록.

완료 기준: 반복 실행과 브라우저 재시작을 포함해 위 핵심 흐름이 동작하고, 저장 결과를 확인할 수 있다. 한 번 성공만으로 장기 안정성을 주장하지 않는다.

진행 판단: 성공하면 P1로 이동. 불안정하면 원인을 좁혀 수정하고, 해결하기 어려우면 원고·이미지 내보내기와 에디터 입력 보조까지 가능한 범위를 사용자에게 제시한다.

최종 판단: P1 진행 가능. 실제 글 생성·저장·재열기 성공, 마지막 재시작 후 동일 글 검증 반복 성공. 장기간 로그인 유지나 범용 업로더 완성으로 해석하지 않는다.

### P1. 로컬 웹앱 기반

- [x] Next.js UI/API와 작업 실행 프로세스 연결.
- [x] SQLite 스키마·마이그레이션·작업 상태·동시 실행 잠금 구현.
- [x] 설정 화면: 블로그 연결, 연결 상태, 카테고리 새로고침.
- [x] 새 글 화면: 유형, 요약, 폴더/파일 가져오기, 이미지 정렬·캡션·대표 이미지.
- [x] 기본 표지 라이브러리와 글별 이미지 저장.
- [x] 파일 이름 정규화, MIME·크기 확인, 경로 이탈 차단.
- [x] 초안 수동 저장, 작업 진행 표시, 오류 안내.
- [x] 로컬 서버를 loopback에 바인딩하고 변경 요청의 출처/CSRF 보호 구현.
- [ ] 티스토리 검토 중인 두 번째 사진의 최종 게시글 표시 확인. 기존 글만 재확인하며 새 글을 만들지 않음.

완료 기준: AI 없이 입력한 원고를 저장·재편집하고 P0 업로더로 비공개 전송할 수 있다.

현재 판단: 기본 완료 기준은 달성. 전체 사진 표시까지 검증 완료한 것으로 표시하지 않는다. 작업 내역의 ‘저장 결과 다시 확인’은 저장 URL만 읽어 검사하며 검토 중이면 확인 대기 상태를 유지한다.

### P2. 프로젝트 글 생성

- [x] AI 공급자 인터페이스와 ChatGPT 로그인 기반 Codex 공급자 구현.
- [x] 앱 전용 공식 로그인, 모델 목록·한도 확인, 오류·반환 토큰 사용량 처리. API 키 계획은 D15로 대체.
- [x] 요약과 이미지에서 개요·본문·캡션 생성.
- [x] 구조화 원고 검증, 미리보기, 수정, 구간 재작성, 버전 저장.
- [x] 앱의 자동 재시도 없음·실패당 수동 재시도 한 번·5분 생성 제한, 진행 취소, 입력 부족 안내.
- [x] 생성→수정→비공개 저장 전체 흐름 확인. 합성 사진과 프로젝트 요약으로 실제 생성·부분 수정·원고 저장·비공개 전송·재확인 성공.

완료 기준: 프로젝트 글 1개를 실제 입력 자료로 만들고, 사용자 편집 후 티스토리에서 결과를 확인할 수 있다. 미제공 사실은 채우지 않고 누락 사항으로 표시한다.

### P3. 기존 문체 반영

- [x] 대표 글 URL/본문 가져오기, 본문 정제, 유형 지정.
- [x] 문체 규칙 분석, 사용자 수정, 버전 관리.
- [x] 유형별 대표 예시 선택과 생성 요청 반영.
- [x] 기본 문체와 사용자 문체 결과 비교, 사용자 수정본 반영 경로 구현.

완료 기준: 선택한 대표 글의 말투·구조가 반영되는지 사용자가 비교·수정할 수 있다. 문체 일치율 같은 수치를 근거 없이 만들지 않는다.

### P4. 기술·PS 확장

- [x] 기술 주제·해석·자료 입력, 공식 출처 확인 단계 추가.
- [x] URL 접근 실패 시 본문 붙여넣기 지원.
- [x] PS 제약 조건·언어·본인 코드 입력과 해설 템플릿 구현.
- [x] 정당성·복잡도·예제 검토 및 검증 상태 표시.
- [ ] 제한된 코드 실행 환경을 준비하고 예제·경계 사례 실행. 어댑터 구현 및 Docker 미설치 시 미실행 처리 확인; 컨테이너 실실행은 대기.
- [x] 티스토리 코드·표·필요한 수식 표현 확인.
- [ ] 두 번째 AI 공급자 추가 및 동일 입력 계약 확인. D22에 따라 후속 선택 항목으로 보류.

완료 기준: 기술 글과 PS 글 각각 한 편을 작성·검토·전송하고 출처 및 코드 검증 수준을 확인할 수 있다.

### P5. 운영 안정화와 공개 발행

2차 계획에서는 공개 발행을 R6, 운영 안정화를 R0·R7로 나누어 진행한다. 아래 미완료 항목을 삭제하거나 완료로 간주하지 않는다.

- [ ] 공개/비공개 선택과 대상 원고 버전 표시.
- [ ] 중복 요청, 업로드 중단, 저장 직후 응답 유실 복구.
- [ ] 실패 단계별 진단과 민감 정보 마스킹.
- [x] 초안·문체·설정 백업/복원. 인증 정보는 일반 백업과 분리. R0에서 기존 DB 교체 방식, R0.1에서 빈 경로 복원과 복원 부산물 Git 제외 구현·검증 완료.
- [ ] 필요한 경우 사용자 설정에 따른 자동 발행 모드 제공.
- [ ] 예약은 시간대와 PC 절전·재시작 후 지난 작업 처리 규칙을 정한 뒤 구현.

완료 기준: 중단·재시도 시나리오에서 중복 저장을 방지하거나 불명확한 결과를 확인 대기로 남기고, 사용자가 발행 결과를 추적할 수 있다.

### P6. 선택적 확장

네이버 및 여러 발행 대상은 사용자 요청으로 2차 R1·R2·R5에 포함한다. 외부 배포·서버형 다중 사용자 운영은 R7의 후속 선택으로 유지한다.

- [ ] 배포 웹앱 + 로컬 실행 프로그램 페어링 검토.
- [ ] 다중 블로그가 필요하면 프로필·작업·저장소 격리 추가.
- [ ] 서버 실행은 원격 로그인, 세션 보호, 운영 비용을 검토한 후 결정.

착수 조건: P0~P5 결과와 실제 사용 중 필요한 기능을 바탕으로 범위 재합의.

### 배포 준비 점검 (2026-09-22)

- [x] 구현·기존 외부 실측 기록·미구현 로드맵 구분과 로컬/서버 배포 격차 분석.
- [x] 기존 R0~R7을 유지한 실행 로드맵 개정: 복구/배포 기반 선행, 단계별 완료 기준, 출시 단위와 첫 개발 묶음 정의. 계획 작성 완료이며 신규 기능 구현 완료가 아님.
- [x] 단위 15개·타입·빌드·로컬 Chrome 기본 검사, 현재 데이터 감사, 기존 백업 해시 검증, 운영 의존성 취약점 조회.
- [x] 저장 URL이 있는 작업의 중단 후 `unknown` 상태에서 읽기 전용 재확인을 허용하고 합성 중단 상태/모의 게시글로 검증. URL 후보 연결·사용자 미발행 종료도 구현.
- [x] 복원 보존/실패/임시 폴더의 사진을 Git 제외 대상에 포함하고 검증.
- [x] 새 설치의 빈/없는 데이터 경로 복원 구현·격리 검증. 실제 다른 PC 설치는 R7.2에서 별도 확인.
- [x] R7.1 런타임 의존성·최초 블로그 설정·환경 진단·Windows 소스 설치/실행/종료·CI 정의·지원 조건/변경 기록 정리. 별도 신규 폴더의 실제 npm 설치 및 배포 실행 검사 완료.
- [ ] R7.2 타 PC 인수·업데이트/롤백 자동화·비ASCII 설치 경로 호환성·외부 배포 라이선스/고지 확정. GitHub 원격 CI 실행 결과도 업로드 후 별도 확인.
- [ ] 다른 PC/계정에서 생성·전송·중단 복구·복원 전체 인수 검사. 공개 발행을 제공할 경우 R6 별도 완료.

문제 발견 시점의 근거: [2026-09-22 배포 준비 점검](docs/DEPLOYMENT_READINESS_2026-09-22.md). 후속 R0.1·R7.1에서 위 체크 항목을 수정했으며 실제 검증 범위는 아래 작업 기록을 따른다. 설치 안내: [Windows 로컬 설치](docs/WINDOWS_SETUP.md).

## 11. 검증 전략

### 공개·기술 분석·학습 자료화 (2026-09-23)

- [x] 현재 Git/remote 공개 여부·13개 도달 가능한 커밋/138개 blob·공개 대상 및 로컬 민감 경로 검사. 비밀값은 출력하지 않음.
- [x] README 구조/기술/완성도/환경/시연 위치 보강, 값 없는 `.env.example`, IDE·인증서·서비스 계정 제외 규칙 추가. 라이선스 유지.
- [x] 기능·기술·데이터 흐름·부채·보안·테스트·학습/면접·이력서 자료 작성. 코드/검사/기존 외부 이력 구분.
- [x] 타입·기존 테스트 32개·빌드·빌드 추적 검사 및 R3 모의 UI 검사. 합성 GET 증가 측정과 사진 분석 apply 오류 일관성 재현.
- [ ] 사진 분석 결과 적용의 원고/작업 저장 원자성 수정. 이번에는 진단·계획만 수행.
- [ ] 공개 대상 최종 stage 검사·커밋·push 및 원격 확인. 아래 작업 기록에 실제 결과를 갱신한다.

| 검증 | 확인할 내용 |
| --- | --- |
| 빌드·정적 검사 | 타입, 의존성, 설정 오류 |
| 핵심 단위 검증 | 원고 변환의 이미지 참조·코드 보존, HTML 정제, 파일 경로 검증 |
| 작업 통합 검증 | 중복 요청, 프로세스 중단, 잠금 복구, 저장 결과 불명확 처리 |
| 공급자 모의 검증 | 타임아웃, 사용량 누락, 잘못된 구조화 출력 |
| 실제 티스토리 검증 | 로그인, 이미지, 카테고리, 비공개 저장, 다시 열기 |
| 콘텐츠 검토 | 문체, 사실 근거, 이미지 설명, PS 정당성·복잡도 |

실제 티스토리 저장 검증은 필요할 때만 수행한다. 일반 테스트가 실제 계정으로 자동 발행되지 않도록 분리한다. 외부 서비스 화면 변경에 따른 실패와 코드 결함을 구분해 기록한다.

## 12. 미확정 사항과 확인 시점

| 항목 | 현재 기본값 | 확인 시점 |
| --- | --- | --- |
| 실제 블로그 URL과 테스트 카테고리 | `https://mid-night-coding.tistory.com/`, `테스트` (사용자 지정) | 에디터에서 카테고리 존재 확인 |
| 로그인 및 테스트 글 작성 | 사용자가 직접 로그인, 비공개 기준 | P0 실행 시 실제 계정 연결 |
| 첫 AI 공급자·모델 | ChatGPT 로그인 기반 Codex, 모델은 계정 목록에서 선택 | P2 실제 연결·이미지 입력 확인 |
| API 키·비용 한도 | API 키 사용 안 함. 구독 한도를 확인해 소진 또는 확인 불가 시 생성 거절 | 매 생성 직전 확인. 별도 결제/크레딧 구매 요청 없음 |
| 문체 대표 글 | 사용자가 고른 글 우선 | P3 시작 전 |
| SC의 구체적 의미와 기술 분류 | 유형명 자유 입력 | 기술 입력 UI 구체화 시 |
| PS 주 사용 언어·사이트 | 우선 Python과 자체 연습 문제로 검증, 사용자 선호 언어 미확정 | 다른 언어 실행 확장 전 |
| 공개 자동 발행·예약 필요 수준 | 초기 비공개 저장 | P5 시작 전 |

미확정 사항은 의존 작업에 도달했을 때 확인한다. 문서 작성과 기반 구현 등 독립 작업을 불필요하게 막지 않는다.

## 13. 근거 자료와 확인 한계

2026-09-09에 앞선 가능성 조사에서 확인한 자료다. 아래는 도구 기능과 설계의 근거이며 실제 티스토리 자동화 성공을 증명하지 않는다. 의존성 구현 시 해당 공식 문서를 다시 확인한다.

- 티스토리 Open API 종료 공지: https://notice.tistory.com/2664
- 기존 API 종료 표시: https://tistory.github.io/document-tistory-apis/apis/
- Playwright 인증 재사용: https://playwright.dev/docs/auth
- Playwright 전용 프로필과 기본 Chrome 프로필 제약: https://playwright.dev/docs/api/class-browsertype#browser-type-launch-persistent-context
- Playwright 입력·파일 첨부: https://playwright.dev/docs/input
- 웹페이지 간 접근 제한: https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Same-origin_policy
- OpenAI 공식 API 시작 문서: https://developers.openai.com/api/docs/quickstart
- Claude API 인증: https://platform.claude.com/docs/en/manage-claude/authentication
- Claude 외부 도구 인증 안내: https://support.claude.com/en/articles/13189465-log-in-to-your-claude-account

## 14. 작업 기록

### 2026-09-09 — 상세 계획 및 문서 체계 생성

- 요청: 상세 구현 계획과 앞으로 읽고 갱신할 작업 문서 작성.
- 변경: 이 문서에 요구사항, 구성, 데이터, 자동화·생성 흐름, P0~P6 계획, 검증 기준 정리.
- 변경: `AGENTS.md`에 작업 전 문서 확인 및 작업 후 갱신 지침 추가.
- 확인: 프로젝트 폴더의 기존 파일 조회에서 파일이 발견되지 않음.
- 검증: 문서 파일 생성과 주요 항목 존재 확인. 앱 코드와 티스토리 실동작 검증은 아직 수행하지 않음.
- 결정: 구현은 AI 없는 비공개 저장 검증부터 시작.
- 남은 작업: P0 초기화와 실제 브라우저 검증.

향후 기록 형식: 날짜 / 요청·목표 / 변경 파일과 동작 / 검증 명령·결과 / 결정·이유 / 실패·제약 / 다음 작업. 체크리스트는 실제 완료한 항목만 표시한다.

### 2026-09-09 — Git 로컬 저장소 준비

- 요청: 작업 내역을 Git에 올리기 위한 연동.
- 확인: Git 2.48.1 설치 및 기존 커밋 작성자 설정 확인. 프로젝트에는 기존 저장소가 없었음.
- 변경: `main` 브랜치로 Git 초기화, `.gitignore`와 `.gitattributes` 추가.
- 제외 대상: API 키 환경 파일, 브라우저 인증 상태, 사용자 자료, SQLite DB, 실행 로그와 테스트 진단 자료.
- 검증: `git check-ignore`로 환경 파일·프로필·자료·의존성 제외 확인, `git diff --cached --check` 통과.
- 결과: 초기 커밋 `b56216e` 생성. GitHub 앱 연결 계정은 `Jinxxlog`로 확인했으며 로컬 Git push 인증은 별도 확인 필요.
- 남은 작업: 사용자 원격 저장소 지정, 인증 확인, 원격 push 및 결과 확인.

### 2026-09-09 — GitHub 공개 저장소 연결 완료

- 요청: `auto_tstory` 이름의 새 공개 저장소 생성 및 업로드.
- 확인: 기존 Git Credential Manager의 GitHub 인증 계정이 `Jinxxlog`임을 확인. 인증 값은 파일이나 로그에 기록하지 않음.
- 변경: GitHub에 `Jinxxlog/auto_tstory` 공개 저장소 생성, HTTPS `origin` 연결.
- 검증: 저장소 생성 응답의 공개 여부 확인, `git push -u origin main` 성공.
- 결과: 계획서 및 Git 설정 파일의 기존 두 커밋 업로드 완료, `main`의 원격 추적 설정 완료.
- 남은 개발 작업: P0 초기화와 티스토리 실동작 검증. Git 연결은 완료.

### 2026-09-09 — P0 실행 기반 및 로컬 검증 완료

- 환경: Node.js 22.20.0, npm 10.9.3, 설치된 Google Chrome 사용.
- 의존성: Playwright 1.63.0, TypeScript 7.0.2, tsx 4.23.13, @types/node 22.20.1. npm 레지스트리에서 버전·엔진 확인 후 lockfile로 고정.
- 변경: `package.json`, `package-lock.json`, `tsconfig.json`, P0 CLI와 브라우저 검사 모듈, URL 검증, 고정 테스트 원고, 테스트 코드, README 추가.
- 동작: `npm run p0`로 `.local/tistory-profile/`의 전용 Chrome 실행. 수동 로그인, 블로그 설정, 상태·DOM 검사, 화면 저장, 재시작, 정상 종료 명령 제공.
- 제한: 실제 에디터 구조 확인 전이므로 본문 입력·이미지 업로드·카테고리 선택·발행 명령은 아직 구현하지 않음. 인증 화면의 DOM과 입력값은 검사하지 않음.
- 검증: `npm install` 성공, 감사 결과 취약점 0개. `npm run typecheck` 통과, `npm test` 3개 통과.
- 검증: `npm run test:browser`에서 로컬 페이지의 한글 입력, 재시작 후 영속 쿠키와 localStorage 유지, PNG 2개 파일 선택 통과.
- 검증: `git diff --check`와 민감 경로의 `git check-ignore` 확인 통과.
- 준비 자료: `.local/fixtures/draft.json`, `draft.html`, `p0-image-1.png`, `p0-image-2.png`. 합성 테스트 자료이며 사용자의 실제 사진은 사용하지 않음.
- 실제 실행: 표시되는 전용 Chrome에서 티스토리 로그인 화면 도달 확인. 아직 로그인 완료 응답과 대상 블로그·카테고리를 받지 못함.
- 중요 구분: 로컬 쿠키 유지 성공은 티스토리 인증 유지 성공을 뜻하지 않음. P0는 아직 완료되지 않음.
- 다음 작업: 사용자 로그인 이후 관리 화면 관찰, 글쓰기·카테고리 구조 확인, 비공개 저장 어댑터 구현과 실동작 검증.

### 2026-09-09 — 실제 에디터 검증 및 발견된 문제

- 사용자 지정 대상: `https://mid-night-coding.tistory.com/`, 카테고리 `테스트`.
- 확인: 수동 로그인 후 관리 홈 접근, 글쓰기 링크를 통해 `/manage/newpost/` 도달.
- 확인: 카테고리 메뉴의 `테스트` 항목 선택 성공. 카테고리 식별 값은 현재 UI에서 관찰했으며 코드에는 이름 선택을 사용.
- 확인: 기본모드의 TinyMCE iframe, HTML 모드의 CodeMirror UI를 관찰. HTML 모드 전환의 알려진 서식 변경 확인창만 처리.
- 확인: 고정 테스트 원고의 제목·문단·목록·코드·표를 키보드 입력으로 작성. 내부 글쓰기 HTTP API나 에디터 내부 상태에 직접 쓰지 않음.
- 확인: HTML 모드의 실제 파일 첨부 UI로 PNG 2장 업로드. 미리보기에서 코드 개행·표·이미지 640×360 두 장과 1→2 순서를 DOM 및 화면으로 확인.
- 확인: 발행 설정창의 `비공개` 라디오와 `비공개 저장` 버튼 확인. 최종 저장은 아직 실행하지 않음.
- 문제 1: 프로필만으로는 로그인 세션이 재시작 후 유지되지 않음. 인증 쿠키 저장·복원 추가 후 한 번 재접속에 성공했으나 이후 재시작에서 다시 로그인 필요.
- 조치 1: 오래된 전체 저장소 스냅샷을 적용하는 대신 프로필의 최신 localStorage/IndexedDB를 유지하고 쿠키만 복원하도록 변경. 인증된 관리·편집 화면에서 갱신된 쿠키를 주기적으로 저장하고 정상 종료 시에도 저장. 실제 안정성은 재검증 필요.
- 문제 2: HTML 모드 본문 이미지가 대표 이미지로 자동 지정되지 않음. 별도 대표 이미지 업로드 후 확인창 발생. 확인창 내용 확보 전 진단 명령이 대기 상태에 빠져 실행 도구 재시작.
- 조치 2: 확인창 객체를 보관해 내용 확인·취소할 수 있도록 변경. 대표 이미지 오류의 구체 원인은 아직 미확정.
- 문제 3: DOM 검사 함수 안의 로컬 함수가 실행 시 `__name` 참조 오류를 일으킴. 직렬화되는 함수 구조를 수정하고 로컬 브라우저 검사 테스트에 회귀 검증 추가.
- 검증: 타입 검사 및 단위 테스트 3개 통과. 로컬 검증에서 세션 쿠키 복원·DOM 검사·인증 화면 검사 차단 확인.
- 상태: P0 부분 성공. 비공개 발행 성공이나 로그인 장기 유지 성공으로 기록하지 않음.
- 후속 조치: 로컬 테스트 프로필의 검사 기록과 이미지를 실제 티스토리 검증 자료에서 격리. `npm run p0:fixtures`를 별도로 제공해 테스트 원고 ID를 유지하도록 변경.
- 후속 검증: 수정된 쿠키 복원 방식의 로컬 브라우저 검증과 별도 fixture 생성 명령 통과. 실제 재로그인은 대기 중.

### 2026-09-10 — P0 검증 마무리

- 요청: 중단되어 보이는 작업을 이어서 마무리. 이전 실행 결과와 현재 파일을 재확인해 완료 기록 정리.
- 실제 저장: 2026-09-09 23:43 KST, 게시글 ID `15`, 카테고리 `테스트`, 비공개. 테스트 글은 1개만 생성.
- 원고 식별자: `P0-2026-09-09T09-38-45-378Z`.
- 확인: 기본모드에서 첫 본문 이미지를 선택하고 `.mce-represent-image-btn`으로 대표 지정. 발행 화면의 대표 썸네일과 첫 이미지 일치 확인.
- 확인: 저장 전 원고 제목·본문·이미지 2장·표·카테고리·비공개 라디오·비공개 저장 버튼을 검사한 뒤 실제 UI로 저장.
- 확인: 관리 목록의 카테고리·비공개 표시와 수정 URL에서 글 ID 식별. 글을 다시 열어 제목·문단·목록·코드 개행·표·이미지 2장·대표 이미지를 검사.
- 확인: 화면에서 이미지 1→2 순서와 첫 이미지가 게시글 상단 대표 이미지로 표시됨을 확인.
- 확인: 별도 비로그인 컨텍스트로 같은 글에 접근하면 권한 없음 페이지 표시. 기존 로그인 세션에는 영향 없이 종료.
- 확인: 최신 쿠키 저장 후 브라우저 재시작 → 관리 화면 → 저장된 글 재열기 → 동일 내용 검사 전부 통과. 마지막 검증에서 추가 로그인 불필요.
- 확인: 기존 글의 수정 화면에서도 HTML→기본모드 전환과 내용·이미지 보존 확인. 추가 저장은 실행하지 않고 검증 브라우저 정상 종료.
- 변경: 비공개 저장·결과 확인 명령, 저장 시도 journal 추가. 저장 버튼 클릭 전 배타적으로 journal을 생성해 같은 원고의 중복 시도 방지. 결과가 불명확하면 자동 재발행하지 않음.
- 변경: 임시 좌표 진단과 불필요한 DOM 출력, 실패한 별도 대표 이미지 업로드 명령 제거. 현재 단계별 명령과 제약을 README에 반영.
- 제한: 최종 대표 이미지 경로 검증에는 현재 스킨의 `.article-header`를 사용. 다른 스킨에서는 검증 어댑터 조정 필요.
- 제한: 이전 재접속 실패의 모든 서버 측 원인을 규명한 것은 아님. 최종 방식의 단기 재접속은 성공했지만 세션 만료 시 직접 로그인하는 흐름을 유지.
- 증거 파일 (Git 제외): `.local/publication-P0-2026-09-09T09-38-45-378Z.json`의 `verified` 및 전체 검사 true, `.local/saved-post.png`, `.local/saved-image-1.png`, `.local/saved-image-2.png`, `.local/anonymous-post-result.json`, `.local/missing-session-result.json`.
- 완료: P0 가능성 검증. 다음 구현 단계는 P1.
- 최종 로컬 검증: `npm run typecheck` 통과, `npm test` 3개 통과, `npm run test:browser`의 입력·영속/세션 쿠키·저장소·DOM 검사·파일 선택 전부 통과.

### 2026-09-10 — P1 로컬 웹앱 및 비공개 저장 구현

- 요청: 로컬 웹앱 기반 구현 및 중단된 작업 마무리. 사용자가 직접 로그인한 전용 Chrome을 유지하면서 실제 저장을 검증.
- 변경: Next.js 16.3.4·React 19.2.8 UI/API, 별도 Node 실행기, SQLite WAL·마이그레이션·실행 임대·작업 스냅샷 구현. `npm run dev`로 웹 서버와 실행기를 함께 시작.
- 변경: 유형·제목·요약·Markdown 편집과 미리보기, 저장·재편집, 사진/폴더 가져오기·캡션·순서·대표 이미지·표지 라이브러리, 연결·카테고리·작업 내역 제공.
- 변경: 이미지 실제 디코딩·MIME/크기 검사·PNG 변환·메타데이터 제거, 경로 검증, loopback 바인딩·변경 요청 Origin/헤더 검사. 원고 동시 편집은 버전 충돌로 거절.
- 변경: 발행 버전별 중복 방지 및 저장 요청 이후 불명확한 결과의 자동 재발행 차단. 저장 URL이 있으면 같은 작업의 읽기 전용 재확인 기능 제공.
- 실제 저장: 지정 블로그의 ‘테스트’ 카테고리에 새 P1 비공개 글 1개 저장. 원고 식별자는 `2026-09-09T15:36:06.086Z`, 작업 ID는 `f70fc5bd-68d1-4749-a16d-beab9133bd9d`. 저장 URL은 로컬 작업 내역에 보관.
- 실제 확인: 관리 목록의 비공개·카테고리, 재편집 화면의 제목·본문·사진 URL 순서·캡션 유지. 게시글 화면의 코드·표·첫 사진·대표 이미지 확인. 별도 비로그인 컨텍스트에서 권한 없음 표시 확인.
- 미완료: 두 번째 합성 사진이 티스토리의 법령 관련 사진 검토 안내로 대체되어 표시됨. 업로드·저장은 되었으나 최종 사진 표시 성공으로 기록하지 않음. 작업은 `needs_attention`이며 기존 글만 재확인하는 버튼 동작을 검증. 검토를 우회하거나 추가 글을 만들지 않음.
- 검증: 최종 `npm run build`, `npm run typecheck`, `npm test` 7개 통과. `npm run test:browser`의 로컬 입력·세션 복원·파일 선택 검사 통과.
- 검증: 로컬 웹앱에서 수동 저장·새로고침·재편집, 코드·표 미리보기, 파일 가져오기 확인. `npx tsx scripts/web-assets-check.ts`의 라이브러리·폴더 가져오기·순서/캡션 유지·모바일 가로 넘침 없음·CSRF 거절 통과. 웹 검증은 실제 전송 스크립트와 분리.
- 증거 파일 (Git 제외): `.local/p1-home.png`, `.local/p1-editor.png`, `.local/p1-mobile.png`, `.local/p1-saved-post.png`, `.local/p1-anonymous.json`. 최종 사진 전체 검증 파일은 성공하지 않았으므로 완료 증거로 사용하지 않음.
- 제한: Node.js 22 내장 SQLite의 실험적 경고가 있음. 실제 대표 이미지 검사는 현재 스킨 기준이며 사진은 본문 끝에 배치. 장기 로그인 유지와 다른 계정·스킨의 동작은 미검증.
- 다음 작업: 사진 검토 완료 후 저장 결과 재확인. 다음 개발 단계 P2에서 AI 공급자·키 설정과 프로젝트 초안 생성 구현.

### 2026-09-11 — P2 ChatGPT 로그인 기반 프로젝트 글 생성

- 요청·결정: 별도 API 요금 부담 없이 기존 ChatGPT 계정으로 사용하려는 사용자 요청에 따라 공식 Codex App Server 연동 채택. D05의 첫 API 공급자 계획을 D15로 변경.
- 연결: 설치된 Codex CLI 0.153.4의 생성 프로토콜 타입과 공식 문서 확인. `.local/codex-writing/`의 전용 인증 저장소에서 사용자 직접 로그인 후 ChatGPT Plus와 사진 입력 모델 목록 확인. API 키를 받거나 별도 결제를 요청하지 않음.
- 변경: `src/services/ai/`의 공급자 계약·stdio 요청/알림 처리·계정/모델/한도 조회·생성/취소, `src/lib/ai-store.ts`의 AI 작업/결과·재시도·버전 적용, `src/app/ai-panel.tsx`의 연결·생성·검토·부분 재작성·버전 보기 UI 추가.
- 동작: 원고 요약·사진에서 개요·본문·캡션·부족한 정보 생성. JSON 구조·사진 ID·길이를 검사한 뒤 결과를 별도 보관. 원고 버전이 같을 때만 사용자 적용으로 새 버전을 저장. 부분 재작성은 선택한 유일 구간 밖의 본문을 유지.
- 실제 생성: 사진의 ‘P0 TEST IMAGE 1’ 문구와 파란색 합성 이미지 용도를 읽어 초안·캡션 생성 성공. 최초 생성 반환 사용량은 입력 5,840·출력 378·총 6,218 토큰. 사용량은 청구 금액이 아님.
- 실제 수정: 생성 결과를 v2로 적용, 선택 문단을 재작성해 v3 적용, 제목을 수동 편집해 v4 저장. 선택 구간 밖의 본문 유지 확인.
- 실제 티스토리: 작업 `9f66cd57-e6b7-4df2-bdac-ed41df37b2da`, 게시글 ID `17`, ‘테스트’ 카테고리의 새 비공개 글 1개 저장. 재열기 제목·본문·사진 순서 검사 성공. 별도 읽기 탭에서 사진 크기·캡션·대표 이미지 확인 성공. 인증된 화면의 사진 표시 및 비로그인 접근 차단도 확인.
- 발견·수정: 재편집 화면의 beforeunload를 취소하면 결과 확인 이동이 `ERR_ABORTED`로 실패. 기존 편집 내용을 버리지 않도록 저장 결과 검사를 별도 읽기 탭으로 변경. 같은 저장 URL만 재검사했으며 중복 발행 없음.
- 보호: 앱의 자동 재시도 없음, 실패당 수동 재시도 한 번, 생성 5분 제한, 취소 및 중단 복구, 사용자 수정과 충돌 시 적용 거절. 한도가 소진되거나 확인되지 않으면 생성 거절. 쉘·브라우저·플러그인 등의 기능 비활성화 및 도구 실행 요청 거절. Codex 내부 네트워크 복구와 앱의 재시도 횟수는 별개.
- 검증: `npm test` 9개 통과(버전 보존·중복 적용 거절·수정 충돌·선택 구간 보존·취소 후 늦은 응답 무시·재시도 상한·중단 복구 포함), 타입 검사와 프로덕션 빌드 통과.
- 웹 검증: 실제 AI 생성·사진 입력·결과 적용, 모바일 가로 넘침 없음 확인. 기존 수동 원고 저장/재편집 및 표지·폴더·순서·캡션·CSRF 검사 통과. 새 부분 재작성 입력란과 충돌하던 테스트 선택자를 본문 편집기로 명확히 하고 파일 가져오기 완료 후 검사하도록 보완.
- 취소 검증: 실제 실행 중인 AI 작업에 취소를 요청해 `cancelled` 상태 전환 및 원고에 결과 미적용 확인 (`.local/p2-cancel-check.json`).
- 증거 (Git 제외): `.local/p2-generated-result.json`, `.local/p2-rewrite-check.json`, `.local/p2-publication.json`, `.local/p2-final-check.json`, `.local/p2-generated.png`, `.local/p2-applied.png`, `.local/p2-mobile.png`, `.local/p2-saved-post.png`.
- 제한: 사용자 원본 프로젝트 자료가 아닌 합성 사진과 구현 사실 요약으로 검증. 모든 생성의 사실성·문체 일치를 보장하지 않으며 사용자 검토 유지. 사진은 본문 아래 배치. App Server는 실험적 인터페이스이며 CLI 업데이트 시 재검증 필요. 사용 한도 도달 오류의 실제 서버 강제 재현은 수행하지 않음.
- 완료 판단: P2 프로젝트 생성·수정·비공개 저장 흐름 검증 완료. 다음 단계 P3 기존 글 문체 분석·반영.

### 2026-09-11 — P3 기존 문체 반영 및 중단 복구

- 요청: 기존 글 문체 분석·반영을 구현하고 중단된 작업을 확인해 계속 진행.
- 복구 지점: 대표 글·문체 모델/저장소와 생성 프롬프트 변경까지 작성된 상태를 확인. 분석 실행기·API·문체 관리 화면·생성 화면 연결과 검증을 이어서 완료.
- 변경: `src/services/style/`의 공개 글 추출·문체 분석 프롬프트·결과 검증·예시 선택, `src/lib/style-store.ts`의 대표 글·분석 작업·확정 문체 버전 저장, `src/app/style-panel.tsx`의 가져오기/붙여넣기·유형·분석·규칙 수정·버전 이력 구현. Cheerio 1.2.0 고정 추가.
- 변경: 기존 Codex 연결·한도 확인·취소·5분 제한을 문체 분석에서도 재사용. 같은 실행기 임대 아래 분석과 생성을 직렬 처리. 앱의 자동 재시도 없이 실패당 한 번 수동 재시도.
- 변경: AI 생성 화면에서 기본/확정 문체 선택, 사용한 문체 버전과 예시 수 표시, 두 생성 결과를 나란히 비교. 직접 수정한 원고는 명시적으로 대표 글로 가져와 재분석할 수 있으며 자동 편입하지 않음.
- 자료: 기존 지정 블로그의 공개 글 중 ‘배열/문자열’, ‘리스트 컴프리헨션’, ‘map 함수’ 기술 글 3편을 선택. 테스트 글 제외. 추출 본문은 각각 2,613자·2,193자·1,379자. 원문은 `.local/` 및 로컬 DB에만 보관하고 Git에 넣지 않음.
- 실제 분석: 대표 글 3편에서 정의·예제·설명 순서, 짧은 소제목, 일부 ‘●’ 표시, 존댓말/평서형 혼용 등을 분석. 기술 글 3편의 제한된 표본이라는 경고와 관찰 근거가 반환됨.
- 실제 편집: 분석 결과를 문체 v1로 확정하고 ‘● 소제목’ 보완 규칙을 추가하여 v2 저장. 이는 수정·버전 보관 흐름을 검증한 것이며 사용자가 최종 선호를 확정한 것으로 간주하지 않음.
- 실제 비교: 같은 프로젝트 요약·동일 원고 버전·동일 모델에서 기본 문체와 문체 v2로 각각 생성. 기본 결과의 평서형과 사용자 문체 결과의 존댓말·‘●’ 소제목 차이 확인. 문체 버전 2 및 대표 예시 3개가 생성 요청에 고정됨. 새 사실·경험을 참고 글에서 옮기지 않도록 프롬프트에서 구분했고 이번 결과에서 제공된 구현 범위를 유지함. 문체 일치율을 계산하거나 보장하지 않음.
- 실제 화면: 공개 글 가져오기·저장, 분석 요청, 규칙 수정·확정, 생성 결과 비교, 모바일 가로 넘침 없음 확인. 자동 검사에서 입력란 이름을 정확히 찾도록 문체 규칙·대표 글 유형에 명확한 접근성 이름 추가.
- 검증: 타입 검사·프로덕션 빌드·단위 테스트 11개 통과. 추가 검사는 본문/광고 분리, 허용 URL 경계, 원문 수정/제외 후 스냅샷 유지, 문체 버전 충돌, 유형 불일치 거절 및 프롬프트 반영을 확인.
- 증거 (Git 제외): `.local/p3-profile-result.json`, `.local/p3-comparison.json`, `.local/p3-style.png`, `.local/p3-style-mobile.png`, `.local/p3-comparison.png`. 분석·비교에는 승인된 ChatGPT 구독 한도를 사용했으며 P3 검증에서 티스토리 글을 발행하지 않음.
- 한계: 현재 가져오기는 설정 블로그의 개별 공개 글과 알려진 본문 선택자 기준. 다른 스킨·비공개 글은 붙여넣기 제공. 공통 문체도 현재 기술 글 표본에서 추론한 것이므로 프로젝트·PS 대표 글을 추가해 보완 가능. 사진은 글 끝 배치 유지.
- 완료 판단: P3 요구 흐름 구현 및 실제 분석·수정·생성 비교 검증 완료. 다음 단계 P4.

### 2026-09-11: P4 기술·PS 생성과 비공개 저장

- 구현: 기술 주제·해석, PS 문제·조건·언어·코드·예제 입력을 원고 버전과 함께 보관. 공식 문서/문제 사이트의 HTTPS URL 가져오기, 실패 시 붙여넣기, 최대 5개 자료 선택 및 생성 시 스냅샷 고정. 붙여넣기 입력으로 공식 확인 상태를 위조할 수 없도록 서버에서 출처 상태 결정.
- 생성: 기존 ChatGPT 연결로 기술·PS 전용 구성과 문체 적용 지원. 정당성·복잡도·예제 추적 지침, 출처 확인 수준과 코드 미실행 안내를 결과에 포함. 기존 프로젝트 원고와 부분 재작성 흐름 유지.
- 코드 검증: Python Docker 실행 어댑터와 대기열·버전/코드/예제 스냅샷·결과 표시 구현. 환경이 없으면 미실행 종료. 현재 PC에는 Docker가 없어 실제 코드 실행·격리 제한의 런타임 검증은 수행하지 않았음.
- 실제 생성: Python bisect 공식 문서를 가져와 기술 글 1편 생성, 자체 정수 배열 합 문제로 PS 해설 1편 생성. 각각 v2 적용. PS의 불변식·O(N) 시간/공간 설명과 예제 기대값은 원고 검토로 확인했으며 실행 검증과 구분.
- 웹 검증: 두 유형 입력·저장, 코드·표 미리보기, 모바일 390px 가로 넘침 없음 확인. 같은 제목 자료를 고유 ID로 선택하도록 검사 보완. 긴 코드가 grid 최소 너비를 늘리는 문제 수정.
- 티스토리: 지정 블로그의 테스트 카테고리에 기술 글 ID `18`, PS 글 ID `19` 비공개 저장 후 다시 열기 성공. 본문 코드의 내용·줄바꿈과 표 셀 비교 통과. 비로그인 접근 차단과 일반 텍스트 복잡도 수식 표시 확인. 관련 글 목록을 표 비교에서 제외하고, 연속 전송에서 읽기 탭이 편집기로 재사용되면 새로운 읽기 탭을 열도록 보완. 확인 실패 시 같은 저장 URL만 재확인하여 중복 발행하지 않음.
- 검사: 타입 검사, 단위 테스트 13개, 프로덕션 빌드 통과. `scripts/web-content-check.ts` 입력·화면·미실행 상태 검사 통과. 실제 생성은 단계별 브라우저 검사로 기술·PS 모두 확인. 최초 전체 생성 스크립트는 중복 제목 자료 선택 검증에서 중단되어 기존 결과를 재사용했음.
- 증거(Git 제외): `.local/p4-test-ids.json`, `.local/p4-technical-output.json`, `.local/p4-ps-output.json`, `.local/p4-publish-results.json`, `.local/p4-*-mobile.png`, `.local/p4-*-saved.png`.
- 완료 범위: P4 핵심 작성·검토·비공개 전송 흐름 완료. Docker 실제 실행 검증은 미완료, 두 번째 공급자는 비용 제약에 따라 보류. LaTeX와 다른 언어 실행은 지원하지 않음.

### 2026-09-11: 개발 로드맵 2차 수립

- 요청: 네이버 블로그, Gemini 계정 로그인, 여행·정보 글과 사진별 설명 확장, 비공개 전용 설계 이유 및 남은 작업 재정리.
- 변경: `ROADMAP_V2.md`에 현재 기능/미완료 항목 대응표, 공식 자료 조사, R0~R7의 의존성·작업·완료 기준 작성. 사진 중심 편집을 일반 글·다중 플랫폼의 공통 기반으로 제안하고, 기존 P5/P6의 순서를 재배치.
- 조사: 네이버 공식 API 목록에서 필요한 완전한 글쓰기 경로는 확인하지 못해 Playwright 실험을 제안. Gemini 공식 Google 로그인·headless 문서와 OAuth 재사용 제한을 확인해 공식 CLI 경로의 적합성과 실동작을 별도 검증 대상으로 기록. 계정별 한도나 사진 연동 성공을 단정하지 않음.
- 결정: D24~D26 추가. Gemini 보류를 가능성 검증으로 전환하되 추가 API 결제 없는 방향 유지. 공개 발행/기존 글 공개 전환/예약·무인 발행을 구분.
- 검증: 문서 링크·단계 의존성·기존 미완료 항목 반영 여부 확인. 문서만 변경했으므로 앱 테스트·빌드·네이버/Gemini 로그인·외부 발행은 이번 작업에서 실행하지 않음.
- 다음 작업: R0 백업·회귀 기준, R1 연동 실험, R2 사진/본문 블록. 실제 신규 기능은 미구현.

### 2026-09-11: 2차 R0 기존 기능 정리

- 구현: `node:sqlite` 온라인 백업으로 WAL을 포함한 일관된 DB 스냅샷을 만들고, 스냅샷의 Asset 목록에 해당하는 이미지 파일만 복사. DB·이미지 SHA-256, 크기, 테이블 개수, 마이그레이션을 `manifest.json`에 기록하고 매 검증·복원 전에 대조.
- 복원: 손상된 manifest·DB·이미지를 거절. 웹앱 실행기 또는 대기/실행 작업이 있으면 거절. 복원 직전 별도 안전 백업과 `data.before-restore-...` 폴더를 남기며 교체 후 DB 검증 실패 시 기존 폴더로 복귀하도록 구현. 실제 사용자 데이터 교체는 수행하지 않고 격리된 임시 데이터로 왕복 복원을 검증.
- 범위: 원고·버전·이미지·문체·참고 자료·AI 결과·작업 이력 포함. `.local`의 티스토리/ChatGPT 인증과 진단 로그 제외. `backups/`도 Git 제외.
- 감사: 실제 로컬 데이터의 SQLite 무결성 정상, 원고 13개·버전 20개·이미지 23개 확인. DB에 있지만 파일이 없는 이미지는 0개. 확인 대기 블로그 작업 1건은 오류로 숨기지 않고 P1 사진 재확인 항목과 함께 표시.
- 실제 백업: 형식 v2의 `backups/20260911T094221Z-481c3db3` 생성 후 manifest·DB·이미지 해시 검증 통과. 복원 시 재확인이 필요한 연결 상태와 실행기 임대는 백업에서 초기화하고 인증·진단은 제외. 사용자 콘텐츠가 있으므로 Git 제외. 앞서 개발 중 만든 형식 v1 백업은 현재 복원 후보 목록에서 제외된다.
- 회귀 기준: `REGRESSION_CHECKLIST.md`에 로컬/브라우저/외부 계정 검사를 분리하고 실제 AI 한도·블로그 발행을 사용하는 명령을 기본 검사에서 제외. 중단 상태는 블로그 `unknown/needs_attention/needs_login`, AI·문체·코드 작업별 상태로 구분.
- 검사: 타입 검사, 프로덕션 빌드, 단위 테스트 15개 통과. 새 검사는 일관된 DB/이미지 백업, 복원 전 안전 복사, 왕복 복원, 해시 변조 거절, 작업 중 백업 거절, 실행기 가동 중 복원 거절, 복원 데이터 감사를 확인.
- 미완료: P1 기존 글 사진 재확인과 Docker Python 실제 실행은 외부 상태가 필요한 독립 항목으로 유지. 이번 R0에서 새 AI 생성이나 블로그 저장은 수행하지 않음.
- 완료 판단: R2 데이터 마이그레이션 전에 필요한 R0 핵심 백업·복원과 회귀 기준 확보 완료. 다음 단계 R1.

### 2026-09-11: 2차 R1 네이버·Gemini 연동 가능성 검증

- 네이버 구현: 티스토리와 분리된 영속 Chrome 프로필·인증 쿠키 저장, 로그인/글쓰기 프레임 탐색, 고정 합성 원고 입력, 사진 2장 업로드, 사진별 캡션, 하위 카테고리 선택, 비공개 상태 확인, 단일 발행 시도와 로컬 결과 기록, 인증·비로그인 재확인 명령을 `src/services/naver/probe.ts`와 `scripts/r1-naver.ts`에 추가.
- 네이버 실제 결과: 사용자 로그인 후 `강좌` 카테고리에 `[R1 테스트] 네이버 자동화 검증` 비공개 글 1개 저장. 문단 3개와 사진·캡션 2쌍의 교차 순서, 첫 사진 대표 표시, 재열기 화면의 카테고리·비공개 표시를 확인. 쿠키 없는 컨텍스트에서 제목과 식별자가 보이지 않고 블로그 홈으로 이동함을 확인.
- 네이버 복구 검증: 최종 발행 클릭 뒤 자동 화면 전환 판별이 시간 초과했지만 저장 시도 기록을 먼저 만든 덕분에 재발행하지 않음. 현재 URL과 제목으로 같은 글이 이미 생성됐음을 확인해 결과 URL을 복구한 뒤 읽기 검사를 완료. 후속 저장 대기는 직접 글 URL 이동과 프레임 전환 중 하나만 성공해도 인식하도록 보완.
- 네이버 판정: 조건부 지원 가능. 공식 전체 글쓰기 API가 없고 SmartEditor가 별도 도구를 지원 대상으로 안내하지 않아 UI 변경·추가 인증·CAPTCHA·서비스 정책 위험을 제품에 표시해야 함. 선택자·사진 수·카테고리·비공개 상태가 다르면 저장하지 않고, 저장 뒤 불명확하면 재발행 없이 기존 글만 조회한다.
- Gemini 실제 결과: 공식 Gemini CLI `0.59.0`을 임시 고정하고 별도 홈에서 Google 로그인을 완료했으나 개인용 클라이언트 지원 종료와 Antigravity 이전 오류로 텍스트 생성에 도달하지 못함. 공식 Antigravity CLI `1.2.1` 바이너리의 공개 SHA-256을 확인하고 약관·데이터 사용 선택 화면까지 확인했으나 선택적 데이터 공유에는 동의하지 않고 종료.
- Gemini 판정: 소비자 로그인 기반 앱 연동 보류. Antigravity headless 기능은 기술적으로 존재하지만 소비자 추가 약관이 제3자 소프트웨어·도구를 통한 접근과 비Google 제품과의 결합을 제한한다. 사진·다중 이미지·스키마·부분 재작성·문체 분석은 실행하지 않았으며 성공으로 기록하지 않음. 임시 Gemini 패키지는 제거했고 유료 API/Vertex AI로 자동 전환하지 않음.
- 문서: `docs/R1_INTEGRATION_REPORT.md`에 실측 결과, 공식 근거, 조건부 지원 경계, R2·R4·R5 영향을 정리. `ROADMAP_V2.md`의 R1 체크리스트는 실제 성공과 정책상 미실행을 구분해 갱신.
- 검사: 네이버 저장 결과 `verified-private`, 로그인 상태의 제목·본문·사진 2장·캡션·순서·카테고리·비공개 표시와 비로그인 차단 모두 통과. 최종 타입 검사, 단위 테스트 15개, 프로덕션 빌드 통과. 데이터 감사는 DB 무결성 정상과 기존 P1 확인 대기 작업 1건을 그대로 보고.
- 검증 증거(Git 제외): `.local/r1-naver-publication.json`, `.local/r1-naver-page.png`, 네이버 인증 프로필·쿠키, 임시 Gemini/Antigravity 검증 자료. 계정 인증 값과 비공개 글 URL은 공개 Git에 넣지 않음.
- 완료 판단: R1의 목표인 두 연동의 성공/조건부/보류 판정 완료. 다음 단계 R2 사진 중심 원고·공통 연결 구조로 진행할 수 있다.

### 2026-09-22: 현재 완성도와 배포 준비 분석

- 요청: 블로그 자동 글쓰기 프로젝트 진행 상황과 배포까지 필요한 작업 분석.
- 조사: 계획·2차 로드맵·회귀 기준과 실제 API/작업 실행기/저장소/AI 연결/백업/배포 스크립트를 대조. P0~P4 핵심과 R0 완료, R1 판정 완료, R2 이후 미구현을 구분. 네이버는 별도 CLI 실험이며 웹앱 정식 기능은 아님.
- 검사: `npm test` 15개, `npm run typecheck`, `npm run build`, `npm run test:browser` 통과. `npm run audit`에서 DB 무결성 정상, 원고 13개·버전 20개·이미지 23개·블로그 작업 9개, 이미지 누락 오류 없음과 재확인 필요 작업 1건 확인. `npm run backup:list`에서 기존 백업 1개의 DB/이미지 검증 통과.
- 의존성: `npm audit --omit=dev --json` 첫 조회는 실패. 공식 레지스트리·프로젝트의 제외된 로컬 캐시를 지정한 재조회에서 운영 의존성 취약점 0건 보고. 전체 보안 검증과는 구분.
- 재현: 격리된 합성 DB에서 저장 URL 확보 후 `결과 확인` 단계 중단·임대 재획득 시 `unknown` 전환 및 `verify()` 거절 확인. 합성 백업의 빈 데이터 폴더 복원은 `ERR_SQLITE_ERROR`로 실패. `git check-ignore`로 복원 보존·실패·임시 폴더의 사진 미제외 확인. 실제 사용자 데이터 복원/변경 없이 수행.
- 변경: `docs/DEPLOYMENT_READINESS_2026-09-22.md`에 결과와 배포 차단 항목, 로컬 배포/서버형 서비스의 차이, 범위별 우선순위 제안 작성. P5의 이미 완료된 R0 백업 체크를 실제 이력과 일치시키고 미완료 복원 문제를 별도 체크리스트로 추가.
- 설계 결정: 기존 로드맵과 비공개 발행 범위 유지. 빠른 로컬 베타와 전체 확장 제품의 순서는 제안으로만 기록했으며 사용자 선택이나 구현 완료로 간주하지 않음.
- 한계: 이번에는 앱 UI 전체 회귀·AI 생성·로그인·블로그 기존 글 재확인/신규 발행·Docker 실제 실행·새 PC 설치를 수행하지 않음. 외부 서비스 정책도 재조사하지 않음. 기능 코드 변경·커밋·push 없음.
- 다음: 배포 목적 작업 착수 시 재확인 복구·복원 폴더 보호·신규 설치 복원을 먼저 처리. 기존 확장은 R2에서 이어갈 수 있음.

### 2026-09-22: 기존 계획을 반영한 개발·배포 로드맵 개정

- 요청: 기존 계획을 읽었는지 확인하고 개발 로드맵 작성.
- 변경: 별도 경쟁 계획을 만들지 않고 `ROADMAP_V2.md`를 개정. 완료된 R0/R1 이력과 기존 사진 블록·여행/정보/자유 글·네이버·공개 발행 요구 유지. R0.1에 발견된 복구 결함을 추가하고 R7을 배포 기반/출시 검증/출시 후 확장으로 분리.
- 구체화: R2 데이터·편집·전송 순서와 마이그레이션/되돌리기, R3 자유 글까지 유형별 인수, R4 수동 가져오기와 자동 공급자 연결 구분, R5 공통 실행기·부분 성공 복구, 출시 단위별 필수 조건, 첫 개발 묶음 작성.
- 결정: D30 추가. 데이터 변경 전에 복구를 보강하고 설치 준비를 앞당김. 첫 배포 대상은 개인용 Windows 로컬 앱으로 유지. 기존 네이버·공개 발행 범위를 삭제하지 않으며 예약·서버형 서비스는 첫 출시 필수 조건에서 분리.
- 검증: 문서 간 다음 작업·단계 의존성·완료 기준·링크와 diff 공백 검사 확인. 이번 변경은 문서뿐이므로 앱 검사·빌드·외부 게시를 재실행하지 않음. 앞선 점검 결과를 새 실행 결과로 기록하지 않음.
- 다음 작업: R0.1 데이터 감사/백업 검증 → 복원 폴더 보호·빈 경로 복원 → 저장 결과 미확정 작업 복구. 아직 구현에 착수하지 않음.

### 2026-09-22: R0.1 복구 보강 구현·로컬 검증

- 요청: 중단된 글 재확인, 복원 폴더 사진 보호, 빈 폴더 복원부터 구현.
- 사전 보호: 실제 데이터 감사 정상과 기존 백업 검증을 확인하고 `backups/20260921T153720Z-51349722`에 원고 13개·이미지 23개 새 백업 생성·검증. 사용자 데이터 복원/교체는 수행하지 않음.
- 작업 복구: `unknown`·로그인 대기에서도 저장 URL이 있으면 같은 작업 ID를 `verify`로 재사용. 후보 주소는 별도 보관하고 검사 전 결과 URL/성공으로 처리하지 않음. UI에 기존 글 주소 입력·읽기 전용 확인·사용자 미발행 종료를 추가. 확인 중 취소·새 버전 우회 발행을 차단하고, 미발행 종료 후 같은 버전은 종료 작업을 유지함.
- 읽기 검증: 블로그/게시글 URL 형식, 관리 목록 제목·주소·비공개·카테고리, 본문·코드·표·사진 개수/크기·캡션·대표 이미지 검사. 목록에서 실제 페이지 링크만 최대 20쪽까지 읽음. 저장 URL/후보가 있는 작업은 발행 어댑터에서도 새 글 생성을 거절. 임대가 만료된 실행기의 늦은 응답이 후속 복구 상태를 덮지 않도록 보호.
- 복원: 없는 경로·빈 폴더 가져오기 지원, DB 없이 다른 파일이 있는 폴더·원본과 겹치는 경로 거절. 기존 DB는 안전 백업과 교체 전 폴더를 보존. 교체 후 검사 실패 시 이전 폴더 복귀, 실패 자료는 별도 보존. 사용자 지정 이름을 포함한 복원/임시 폴더 사진 Git 제외.
- 발견·보완: 회귀 검사 중 Windows 임시 백업 폴더 rename의 `EPERM` 1회 발생. Windows의 EPERM/EBUSY/EACCES에만 최대 4회 추가 재시도를 적용하고 재검사 통과. 반복 실패를 성공으로 숨기지 않음.
- 검사: 단위 22개 통과, 타입 검사·프로덕션 빌드 통과. 추가 검사는 저장 전/업로드 중/저장 요청 후/주소 확보 후/재확인 중의 중단 상태, URL 경계, 로그인 재개, 중복 키 보존, 후보 정정, 미발행 종료, 빈 경로·기존 데이터 복원, 교체 후 오류 복귀, 손상 거절 및 Git 제외를 포함.
- 브라우저: `scripts/web-recovery-check.ts`가 격리 DB·별도 Next 서버·작업 실행기 없이 실제 UI/API를 검사. 모의 티스토리에서 올바른 글·다음 목록 페이지 검사, 잘못된 주소·본문·카테고리·공개 글 거절과 GET 요청만 발생함을 확인. 모바일 390px 가로 넘침 없음·화면 확인. 증거는 `.local/r01-web-*`에 보관.
- 문서: README 복구 사용법, 회귀 기준, 로드맵 R0.1 체크와 다음 단계 갱신. D31에 후보/확정 결과 분리와 미발행 종료의 의미 기록.
- 한계: 실제 티스토리 로그인/기존 글 재확인/신규 발행·AI 생성·Docker 실행·다른 PC 검증은 미실행. 원고 13개·버전 20개·이미지 23개와 기존 확인 대기 1건을 성공으로 바꾸지 않음. 목록 탐색 한도·지원하지 않는 스킨은 확인 대기로 유지.
- 완료 판단: R0.1 구현과 로컬 회귀 기준 완료. 실제 계정 호환성 재검증은 외부 인수 항목으로 유지. 다음 R7.1 배포 기반.

### 2026-09-22: R7.1 배포 기반 구현·새 설치 검증

- 요청: R0.1에 이어 실행 의존성·최초 설정·Windows 실행/종료·CI를 구현하고 새 환경의 수동 원고 저장까지 확인.
- 설치: Node 22.20 이상인 22.x 기준, 소스 폴더에서 `npm ci --include=dev` → 빌드/개인 자료 추적 제외 검사 → `npm prune --omit=dev`. `tsx`를 운영 의존성에 포함하고 Next 설정을 `.mjs`로 전환해 배포 실행에서 TypeScript를 요구하지 않도록 구성. 잠금 파일 버전은 유지.
- 실행: `Install.cmd`, `Start.cmd`, `Stop.cmd`, `Doctor.cmd`와 Node 실행/제어 도구 추가. 웹 서버의 설치별 응답을 확인한 뒤 작업 실행기를 시작하고 준비 상태를 확인. 종료는 인증된 loopback 제어 요청→작업 실행기 IPC 종료/임대 해제→웹 종료 순서. 시간 초과 시 해당 실행기가 시작한 자식만 정리. 중복 실행은 기존 실행을 재사용하며, 포트 충돌의 다른 프로그램은 종료하지 않음. 자식의 원문 출력은 배포 로그에 저장하지 않음.
- 최초 설정: 개발자 블로그 기본값 제거, 빈 설정에서 원고 저장 허용, 블로그 주소 없는 연결/전송 거절. 첫 화면 안내와 주소 저장, Node·설치 경로·실행 의존성·빌드·Chrome·AI 파일·폴더 권한 진단 구현. Codex가 없어도 수동 작성 가능함을 확인.
- 발견·보완: 동적 경로 진단의 빌드 추적 경고 출력 중 Turbopack 내부 오류가 발생해 런타임 경로의 추적 제외 지시를 추가. 중첩 설치에서 상위 잠금 파일을 루트로 잡지 않도록 `turbopack.root` 명시. 별도 검사에서 사용자 data/backups 경로가 빌드 추적에 남은 것을 발견해 명시적 제외 및 `test:build-artifacts` 검사 추가. 최종 빌드 추적 9개의 사용자 데이터·인증·백업 경로는 0개.
- 경로 제한: 한글·공백 폴더 최초 설치에서 Turbopack 경로 오류와 Node CommonJS 판별 오류 발생. 루트 지정 후에도 Node 22.20에서 한글 하위 폴더의 `type: commonjs`를 무시하는 최소 사례 재현(영문/공백 사례 정상). 런타임을 임의 패치하지 않고 현재 지원을 ASCII 전체 경로로 한정하고 설치 전에 안내·거절. 비ASCII 지원은 후속 호환성 항목이며 성공으로 기록하지 않음.
- 새 설치 실측: 허용한 소스만 별도 폴더에 복사해 실제 npm 다운로드/설치·빌드·개발 의존성 제거 실행. 제한된 PATH, 빈 DB, 존재하지 않는 AI 실행 파일 조건에서 TypeScript 패키지 없음/tsx 있음, 웹앱·작업 실행기 기동, 최초 블로그 설정, 합성 원고 저장, 재시작 후 본문 유지 확인. 실제 사용자 데이터/인증을 복사하지 않음.
- 실행 인수: 중복 시작의 같은 인스턴스 유지, 인증 없는 종료 요청 거절, 정상 종료 후 제어 파일/DB 임대 제거, 작업 실행기 강제 종료 시 웹/제어 서버 정리, 다른 3000 포트 소유자 보존 통과. 브라우저 화면 오류 없음, 모바일 390px 가로 넘침 없음과 화면 확인. 브라우저 없는 API 검사 경로도 별도 새 설치로 통과.
- 자동 검사: 타입 검사·단위 25개·프로덕션 빌드·빌드 추적 검사 통과. R0.1 격리 UI/모의 티스토리 회귀도 재실행 통과. 실제 데이터 감사 정상(원고 13·버전 20·이미지 23·블로그 작업 9), 백업 2개 검증 통과, 기존 확인 대기 작업 1건 유지. 로컬 Doctor는 Codex 0.155.0의 버전 문자열만 확인했으며 계정/모델 호환성을 새로 검증하지 않음.
- CI/문서: Windows GitHub Actions에 타입·단위·빌드·개인 자료 추적 제외·새 설치 API 검사 정의. README, Windows 설치 문서, CHANGELOG, 회귀 기준 갱신. 현재 라이선스 상태를 UNLICENSED로 명시하고 외부 배포 전 소유자의 라이선스·제3자 고지 확정 기준 기록. GitHub 원격 CI 실행·커밋·push는 이번 작업에서 수행하지 않음.
- 증거(Git 제외): 최종 브라우저 포함 새 설치 `.local/installation-NvQcUh/` (초기 성공 `.local/installation-wb3izd/`), API 검사 `.local/installation-VVSm1v/`, 복구 회귀 `.local/r01-web-Rhf40k/`. 합성 검사 폴더에는 설치 패키지·빌드·테스트 원고·화면이 남으며 공유 소스에 포함하지 않음.
- 한계: 같은 Windows PC의 격리 소스 설치 검증이며 물리적으로 다른 PC 검증은 아님. 한글 설치 경로, 업데이트/롤백 자동화, 실제 계정 로그인/기존 글 재확인/발행, AI 생성, Docker 실행, 원격 CI와 외부 배포는 미완료/미실행 범위로 유지.
- 완료 판단: R7.1 구현과 지원 경로의 새 설치·수동 원고 저장 인수 완료. 다음 R2a 블록 원고 데이터/마이그레이션. R7.2에서 타 PC·외부 계정·업데이트/롤백과 출시 조건을 확정.

### 2026-09-22: R2 사진 중심 편집 구현·로컬 인수, 실제 전송은 로그인 대기

- 요청: 본문 사이 사진 배치, 작성 메모·캡션·긴 설명 분리, 기존 버전 보존과 사진 10장 원고의 전송 검증.
- 사전 보호: 실제 DB 감사 정상(원고 13·버전 20·이미지 23·블로그 작업 9, 기존 확인 대기 1건) 확인 후 `backups/20260922T010042Z-6f05f926` 생성·검증. 실제 원고를 일괄 변환하거나 복원하지 않음.
- 데이터: `schemaVersion: 2`, 안정적 ID의 본문/사진 블록. 본문 블록 내부에 Markdown 문단·소제목·인용·목록·코드·표를 손실 없이 보관. 사진은 자산 ID 참조와 note/caption/description/group 분리. 기존 원고는 편집 시 메모리 변환, 저장 시에만 새 버전 생성. 이전 JSON·전송 스냅샷 불변, 잘못된 블록/참조/중복/상한은 저장 거절. markdown/images는 서버에서 파생하고 첫 블록 저장에 migrations 6 기록.
- 편집: 본문 추가, 제목 드래그·위아래 버튼, 사진 교체·제외·대표 선택, 블록 순서의 미리보기. 교체 시 블록 ID와 메모·캡션·설명 유지, 제외해도 과거 사진 파일은 보존. 작성 메모와 묶음은 게시 HTML에 포함하지 않음. 모바일 사진 필드의 접근성 이름을 명시하고 화면 확인.
- 업로드: 원고 최대 50장, 파일 10MB 제한·기존 디코딩/메타데이터 제거 유지. 순차 업로드·파일별 진행률·실패 사진 개별 재시도. PNG 자산과 최대 480×360 WebP 썸네일 분리, 이미지 SHA-256/크기 보관. 썸네일은 백업에서 제외하는 재생성 캐시. 미리보기는 지연 이미지 로딩, 목록·편집은 썸네일 사용.
- AI: 블록 ID·순서를 유지하는 전체 출력 계약 및 하나의 본문/사진 설명 안의 부분 재작성 적용. 사진 분석은 선택/묶음별 최대 10장, 공급자·모델·이미지 해시·메모·묶음·프롬프트 버전 캐시. 같은 조건 재사용, 변경 시 무효화, 검토 적용·버전 충돌·취소 후 늦은 결과 차단. 새 photo_jobs/photo_cache도 백업·감사·중단 복구에 포함. 실제 AI 요청은 이번에 실행하지 않았으며 계약/합성 결과 검증과 구분.
- 연결/전송: 플랫폼·블로그 ID·프로필·카테고리·비공개 범위를 전송 스냅샷에 고정하고 AI 공급자·모델/이미지 지원·한도와 분리. 티스토리 지원 서식/네이버 미지원 안내. 티스토리 네이티브 업로드 사진을 본문의 지정 위치로 이동하며 저장 전 전체 순서 검사. 업로드 이미지 경로를 저장해 이후 읽기 전용 재확인에서도 식별자·순서 검사. 코드/표 검사도 원고 유형과 무관하게 유지.
- 단위/빌드: 타입 검사·단위 29개 통과. 새 검사는 과거 버전 원문 불변, 레거시 전송 스냅샷 유지, 원고 변환 실패, 블록 백업/복원, 메모 게시 제외, 사진 위치 변경 탐지, 50장 상한, 사진 ID 삽입 공격 거절, AI 전체/부분 블록 적용과 캐시·취소·오래된 결과를 포함. 프로덕션 빌드·빌드 추적 개인 자료 경로 0개 검사 통과.
- 로컬 UI: `npm run test:blocks`로 합성 단색 PNG 50장(900×600~949×649px)을 실제 UI/API 업로드. 첫 업로드 실패 1건을 주입한 뒤 성공 49장 유지·실패 1장 개별 재시도·50장 저장 통과. 이 환경의 업로드/재시도/저장 약 1.62초이며 고해상도 실사진 성능 보장이 아님. 10장 교차 미리보기, 메모 비게시, 코드/표, 드래그·버튼, 교체·버전 보존, 화면 오류 없음·모바일 390px 가로 넘침 없음 확인.
- 회귀: R0.1 격리 UI/모의 티스토리 읽기 검사 통과. R2 모듈을 포함한 별도 소스 폴더에서 npm ci/build/prune 및 개발 패키지 없는 API 최초 설정·원고 저장·재시작·중복 실행·종료·실행기 오류/포트 충돌 검사 통과. 실제 데이터 감사 개수는 사전 값과 동일.
- 실제 티스토리: 기존 전용 프로필로 로그인 상태 확인을 시도. 샌드박스 네트워크 거절은 승인된 네트워크 실행으로 해소했으나 티스토리 세션이 만료되어 LoginRequired 확인. 사용자 직접 로그인을 요청하고 전용 Chrome에서 15분 대기했지만 완료를 확인하지 못해 브라우저를 정상 종료. 비공개 글 저장 버튼은 누르지 않았고 10장 실제 저장·재열기·비로그인 차단은 **미실행**으로 유지.
- 다음 검사 준비: `scripts/r2-private-check.ts --private-test`는 로그인 후 합성 10장과 교차 문단/코드/표를 별도 `.local/r2-private/data`에 생성하고 기존 테스트 카테고리에 비공개 글 1개만 전송하도록 작성. 작업 기록과 URL을 재사용하고 URL 없는 중단에서는 자동 재발행하지 않음. 스크립트 작성은 실제 전송 성공을 뜻하지 않음.
- 증거(Git 제외): 최종 UI `.local/r2-web-7oknEw/result.json`, 사진 필드/모바일 화면, R0.1 `.local/r01-web-eFmxp4/`, 새 설치 `.local/installation-xcrpZN/`, 로그인 진단 `.local/r2-private/`. 원고·인증·이미지·진단을 공개 Git에 포함하지 않음.
- 문서: README·R2 사용/데이터 전환 안내·회귀 기준·ROADMAP·CHANGELOG 갱신. R2a/b는 구현·로컬 검증 완료, R2c 어댑터는 구현했으나 실제 전송 인수 대기로 분리. R2 전체 완료나 R3 착수로 표시하지 않음. 커밋·push·배포 없음.

### 2026-09-23: R3 유형 확장 및 실제 합성 인수, R2 사진 10장 인수 완료

- 요청/범위: 여행·정보·자유 글, 개요와 사진 배치 검토, 블록별 재작성, 유형별 작성→편집→비공개 전송. 사용자가 합성 자료로 먼저 검증을 선택하고 전용 Chrome에서 재로그인 완료.
- 보호: 사전 감사 정상, `backups/20260923T022357Z-fcafae07` 생성. 기존 사용자 DB는 원고 13개·버전 20개·사진 23개·블로그 작업 9개·기존 확인 대기 1건으로 유지. 합성 AI/전송 검사는 `.local/r3-private/data`와 `.local/r2-private/data`에 격리.
- 구현: 새 3개 유형의 입력·검증·생성 지침. 개요 제목/목적·사진 순서의 로컬 검토, 저장 전후 정규화한 검토 토큰. 새 유형 전체 생성 시 자료와 검토 요구. 블록 ID 재작성, AI 잠금, 메모/캡션 보존, 사진 50장 원고에서 대상 1장만 전달. 숨겨진 이전 유형 자료는 생성에서 제외. 새 필드 원고 저장은 migrations 7, 기존 버전 불변.
- 문체/출처: 대표 글·문체에 새 유형 추가. 블로그/유형 기본값과 원고별 선택, 기존 버전 스냅샷·백업 유지. 정보 글에 출처 보관·확인 시점과 접근/사실 대조 구분. 한국관광공사/정책브리핑의 정확한 HTTPS 호스트를 허용 목록에 추가하되 실시간 추출 성공은 미검증.
- 결함 보완: 저장 정규화로 객체 속성 순서가 달라져 개요 검토가 해제되는 문제를 정렬 토큰으로 해결. 기존 기술/PS 부분 재작성의 저장 결과 재검증에서 안내 문장 삽입이 선택 영역을 바꾸는 경로를 수정하고 여섯 유형의 회귀에 추가.
- 실제 AI: 앱 전용 ChatGPT 연결의 기본 사진 지원 모델 `gpt-6-astra`로 유형별 초안 3회+선택 사진 설명 재작성 3회 완료. 결과의 합성 자료 표시·사실 범위·잠근 도입·메모·수동 캡션 보존을 검토. 사진 순서 변경/설명 수동 편집 후 원고 v4까지 보존. 마지막 입력 필터 보강은 로컬 계약 회귀로 검증했으며 추가 외부 재생성은 하지 않음.
- 실제 R3 전송: 여행·정보·자유 각 1편, 사진 각 2장, 테스트 카테고리 비공개 저장. 편집 화면 재열기와 게시글 본문·문단/사진 위치·식별자·크기·캡션·대표·카테고리·비공개, 비로그인 제목/본문 차단 통과. 브라우저 종료 후 `--verify`로 같은 3개 URL 재확인 통과. 새 글 중복 없음.
- 실제 R2: 사진 10장+코드/표 교차 원고 1개 비공개 저장. 처음 일부 사진이 `pc-image-censoring-v1.gif` 검토 안내로 표시되어 성공 처리하지 않음. 같은 URL의 읽기 전용 재확인에서 10장·식별자·순서·크기·대표·코드/표·비로그인 차단 모두 통과. 안내 문구를 사진 식별자 오류와 구분하도록 개선.
- 검사: 단위 32개, 타입·프로덕션 빌드·빌드 추적 9개 개인 자료 제외 검사 통과. 여섯 유형 저장/생성 적용/재편집·잠금·반복 문장의 ID 재작성·오래된 응답·문체 기본값/백업 복원 포함. 새 UI 검사 3유형 입력→개요 검토→모의 생성→편집→블록 재작성→비공개 큐와 390px 모바일 가로 넘침 없음. R2 50장 업로드+실패 재시도+저장 약 1.698초(작은 합성 PNG), 교체/드래그/버전/모바일 회귀 통과.
- 증거(Git 제외): `.local/r3-private/progress.json`, `review.json`, 격리 DB의 AI 작업/사용량·글 URL; `.local/r2-private/result.json`; 최종 UI `.local/r3-web-4QbEjA/`, R2 회귀 `.local/r2-web-sNmnh7/`. 전용 Chrome과 로컬 UI 서버는 검사 후 종료.
- 판단: R3 구현과 사용자가 선택한 합성 자료 인수 완료, R2 10장 실제 전송 인수 완료. 실제 사용자 여행 메모/원본 사진·새 문체 품질, R2 선택 사진 분석 캐시의 실제 AI, 기존 P1 사진, 다른 PC/계정/스킨, 공개 발행은 완료 주장하지 않음. 문서/로드맵 갱신, 커밋·push·제품 배포 없음. 다음 제품 단계 R5.

### 2026-09-23: 공개 준비·기술 분석·학습 자료화

- 요청: GitHub 안전 공개, 실제 호출 기반 기능/스택/아키텍처/보안/기술 부채 분석, 학습·면접·이력서 자료. 사용자가 기능 수정은 진단을 확인한 뒤 별도로 요청하도록 지정.
- 시작 상태: `main`/`origin/main`의 `f460832` 이후 R0.1·R7.1·R2·R3 구현이 미커밋. GitHub API에서 기존 저장소 공개 및 동일 remote HEAD 확인. 기존 구현을 이번에 새로 개발했다고 기록하지 않음.
- 공개 검사: 자체 패턴/경로 검사에서 공개 후보 및 기존 13개 커밋·138개 고유 blob 비밀값 미탐지. 로컬 5,572개 파일 경로 조사, 큰 파일/의존성/캐시 제외 한계 명시. `.local`의 브라우저 인증·Codex/Gemini OAuth 및 Codex 내부 로그의 JWT 패턴 확인; 값은 출력/문서화하지 않고 기존 Git 제외 유지. 공개 후보에 별도 외부 이미지/폰트/원본 자료 바이너리 없음. 라이선스 신규 추가 없음.
- 변경: README 개요/목적/실제 스택/구조/환경/완성도/문제/시연 위치 보강, 값 없는 `.env.example`, IDE·인증서·서비스 계정 Git 제외. 기술 보고서·공개 검사 기록·핵심 기술별 정의/동작/대안/연습/면접 문답 및 이력서 소재 작성. 기능 코드/실제 사용자 DB·인증은 변경하지 않음.
- 검사: 타입, 추가 noUnusedLocals/noUnusedParameters, 기존 32개 테스트, 프로덕션 빌드, 빌드 추적 9개 개인 자료 경로 0 통과. 운영 npm audit 알려진 취약점 0; 설치 상태에 Sharp 관련 extraneous 2개를 발견했으나 자동 삭제하지 않음. R3 합성 UI와 R0.1 모의 읽기 복구 검사 통과.
- 합성 진단: 원고 본문 각 10,000자로 10/100/1,000개를 만든 격리 DB에서 GET 응답 103,186/1,023,616/10,227,916 bytes, 중앙값 4.69/16.50/168.68ms. 워밍업 2회 제외 10회, 네트워크·React·외부 호출 제외. 사진 분석 apply의 작업 기록 실패를 주입해 오류 반환에도 원고 v1→v2, appliedVersion 없음 재현. 개선 성과가 아니며 수정 전 문제로 기록.
- 증거(Git 제외): `.local/publication-audit.json`, `.local/publication-npm-audit.json`, `.local/analysis-check.json`, `.local/r3-web-Q52GZz/result.json`, `.local/r01-web-Y03goE/`. 토큰/원고를 일반 로그에 추가하지 않음.
- 한계: 실제 AI/블로그 재실행, Docker, 타 PC, 설치 재검증 없음. 기존 외부 성공 기록은 해당 당시의 증거이며 이번 성공으로 바꾸지 않음. 원격 CI 결과와 최종 push는 실제 완료 후 별도 기록한다.
- 다음: 안전한 공개 대상 stage·commit·push 확인. 이후 사용자가 수정 요청하면 사진 분석 적용 원자성부터 보완. R5 제품 로드맵은 유지.

## 15. 다음 작업

이번 분석 후 가장 먼저 검토할 결함: `photoAnalysisStore.apply`의 원고 버전 저장과 appliedVersion 기록을 원자적으로 처리하고 실패 주입 회귀로 검증한다. 현재는 수정 계획만 있으며 사용자 별도 요청 전 기능 코드를 변경하지 않는다. 전체 상태 조회·큐 공정성·진단 보존 정책은 기술 보고서의 후속 우선순위를 참고한다.

원격 저장소: https://github.com/Jinxxlog/auto_tstory (공개). 작업 변경을 확인하고 커밋한 뒤 `git push`로 업로드한다. 민감 파일 제외 규칙은 계속 유지한다.

실행 순서는 [개정 개발 로드맵](ROADMAP_V2.md), 발견한 문제의 근거는 [배포 준비 점검](docs/DEPLOYMENT_READINESS_2026-09-22.md)을 따른다.

1. R0.1 구현·로컬 검증 완료. 이후 변경에서도 중단 복구·빈 경로 복원·Git 제외 회귀를 유지하고, 실제 계정 재확인은 별도로 기록한다.
2. R7.1 구현·로컬 새 설치 검증 완료. CI 파일의 원격 실행 성공은 아직 확인하지 않았으며, 라이선스/고지 최종 선택·타 PC 인수·한글 설치 경로·업데이트/롤백은 출시 전 별도 완료한다.
3. R2 사진 10장 실제 전송과 R3 합성 유형별 실제 생성·편집·전송 인수 완료. 실제 사용자 자료/문체 품질 인수와 선택 사진 분석·캐시의 외부 AI 검증은 별도 항목으로 유지한다. 과거 버전·코드·표·이미지 참조 보존 회귀를 지속한다.
4. R5 → R6 → R7.2: 네이버를 공통 대기열/대상별 발행 이력에 연결하고, 플랫폼별 비공개 검증 이후 공개 발행·기존 글 공개 전환, 다른 PC의 설치/업데이트/복원/발행 인수를 수행한다. 네이버 지연 시 티스토리부터 검증 가능하다.
5. R4는 수동 결과 가져오기부터 독립 진행한다. 소비자 로그인 연동 보류와 유료 API 자동 전환 금지를 유지한다. Docker 실행 검증은 환경 준비 시 수행하며 PS 실행 지원 여부와 일반 글 배포를 구분한다.
6. 기존 P1 사진은 같은 글만 재확인해 R7.2까지 추적한다. R7.3 예약·외부 웹 접근은 로컬 운영이 안정된 뒤 별도로 설계한다.
