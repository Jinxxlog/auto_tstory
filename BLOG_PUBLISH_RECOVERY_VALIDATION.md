# 블로그 게시 중복 방지·장애 복구 검증

실행일: 2026-09-23 (Asia/Seoul). 자동 S1~S6 각 5회, **30/30 통과**. 실제 티스토리 E1~E3는 **최종 3/3 통과**했다. 실제 검증 시도는 4회이며, 최초 E3의 테스트 서버 포트 오류 1회를 포함한다. 합계 34회 중 33회 통과·1회 실패 후 수정 복구다. 실제 생성된 글은 합성 자료를 사용한 비공개 글 3개다.

이 결과는 동일 중복키의 요청 억제, 결과 불명확 상태의 자동 재발행 차단, 기존 글 재확인을 입증한다. 외부 티스토리 저장의 exactly-once 보장은 아니다.

## 환경과 소스 식별

| 항목 | 값 |
| --- | --- |
| OS | Windows / win32 10.0.26200 x64 |
| Node.js | v22.20.0 |
| Playwright | 1.63.0, 설치된 Chrome 채널 |
| 플랫폼·공개 상태 | 티스토리 / 비공개 |
| 실제 계정·스킨 범위 | 현재 지원 계정 1개, 현재 스킨 1개 |
| 실행 당시 기준 commit | `752656a424d2c0eba8c899824b82060ba7ba9e37` |
| 실행 당시 변경 상태 | `dirty: true`. 이번 구현을 포함한 작업 트리에서 실행 |
| 구현 commit | `7e69962a3ef505ebf290eb8f9d2f2cb11641ac30` |
| 결과 JSON | [measurements/blog-publish-recovery-20260923.json](measurements/blog-publish-recovery-20260923.json) |

실행 당시 기준 commit만 체크아웃하면 이번 테스트 코드가 없다. 구현 commit에서 아래 명령으로 재현한다. 구현 commit은 실행 후 소스를 기록한 참조이며, 깨끗한 해당 commit에서 이미 30회를 재실행했다고 주장하지 않는다. 실제 검사 후 하네스의 실패 이력 보존과 worker 익명 로그를 보강하고 타입·단위·복구 UI를 재검사했다.

## 기존 구현 확인

`src/lib/store.ts`는 SQLite WAL, busy timeout 5초, 짧은 `BEGIN IMMEDIATE` 트랜잭션을 사용한다.

| 함수·필드 | 기존 동작과 유지한 정책 |
| --- | --- |
| `enqueue` | `blog:draftId:version:private` 중복키에 UNIQUE 제약. 같은 키는 완료·중단 상태까지 기존 ID 반환. 이전 버전의 미해결 게시 작업이 있으면 새 버전 전송 차단 |
| `acquire` | 15초 임대. 만료 후 남은 running을 자동 queued로 돌리지 않음. 저장 요청/결과 확인 단계는 unknown, 앞 단계는 needs_attention |
| `claim` | 유효한 임대 소유자가 queued 한 건만 running으로 변경 |
| `heartbeat` | worker에서 3초 주기 갱신. 소유권 상실 시 처리 중단 |
| `verify` | 기존 ID·dedupe·원고 스냅샷 유지. kind를 verify로 설정해 읽기 전용 검사를 queued에 등록. 후보 주소는 성공 전에 확정 결과로 취급하지 않음 |
| `id / dedupe` | 작업 식별자 / 원고 버전·대상·비공개 범위 중복키 |
| `state / step` | 운영 상태 / 현재 세부 단계. publish_requested·verifying 상태를 새로 추가하지 않음 |
| `snapshot` | 전송 당시 원고와 블로그, 업로드 사진 식별 경로, 확보 URL 또는 검증 후보 |
| `result` | 확보한 URL. URL 존재 자체가 succeeded를 뜻하지 않음 |
| `resume` | unknown/needs_attention 재게시 거절. 로그인·실패 작업에도 기존 URL이 있으면 verify로 진행 |
| `resolveUnpublished` | 사용자 미발행 확인으로 cancelled 종료. 같은 버전은 여전히 같은 작업. 새 전송에는 명시적인 새 버전 필요 |

`scripts/worker.ts`의 게시 처리를 `src/services/publication-runner.ts`로 추출했다. 운영 worker와 모의/실제 테스트가 같은 claim·단계 저장·예외 처리·완료 경로를 호출한다. 진단 콜백보다 복구 상태를 먼저 저장하고, await 뒤 임대 소유권을 다시 검사한다. 임대를 잃은 이전 worker는 새 worker의 복구 상태를 덮어쓰지 않는다.

`Publisher.publish`는 실제 비공개 저장 클릭 **전에** ‘저장 요청’을 기록한다. 업로드 사진 경로를 고정하고, URL을 발견하면 같은 작업에 저장한다. 재편집·읽기 검증 뒤에만 succeeded가 된다. `Publisher.verify`는 관리 화면의 주소+제목, 카테고리·비공개, 본문/사진 배치·코드·표, 사진 수·식별자·순서·크기·캡션 및 선택된 표지를 검사한다. 사진이 있는 원고인데 저장된 사진 식별 경로가 없으면 이제 성공을 거절한다. 오래된 사진 작업은 이 조건 때문에 확인 대기에 남을 수 있다.

기존 `tests/recovery.test.ts`와 `scripts/web-recovery-check.ts`의 ID 유지, 다른 블로그 거절, 후보 미확정, 명시적 미발행 종료 동작을 유지했다. 기존 복구 UI 검사는 이번에도 통과했다.

## 장애 주입·기록 방식

운영 코드에는 장애 환경변수나 장애 분기를 넣지 않았다. `scripts/recovery/`의 테스트 어댑터만 장애를 주입한다. `NODE_ENV=test`, `--recovery-test`, 허용된 `.local/publish-recovery*` 격리 경로가 모두 필요하며 실제 게시에는 `--private-test`도 필요하다.

| 지점 | 증거 |
| --- | --- |
| 외부 저장 요청 전 종료 | S2 자식 프로세스 IPC 장벽에서 부모가 실제 종료 |
| 저장됐으나 응답 유실 | S3 외부 저장 모의 원장에 1건 기록 후 예외 |
| 생성 후 URL 저장 전 종료 | S5 실제 자식 프로세스 종료 |
| URL 확보 후 검증 전 종료 | S4 실제 자식 프로세스 종료 |
| 검증 요청 오류 | S4 Playwright 요청 abort → needs_attention → 명시적 재검증 |
| worker 처리 중 프로세스 종료 | S2/S4/S5 합계 15회, 실제 15초 임대 만료 후 새 프로세스 취득 |
| 실제 저장 완료 통지 유실 | E2 실제 저장·관리 화면 이동 직후, URL 기록 전 테스트 예외 |
| 실제 앱·worker 재시작 | E3 URL 기록 후 예외로 needs_attention, worker 종료, Next 서버 종료·재실행 및 새 worker에서 복구 |

E2는 **실제 저장 성공 후 애플리케이션 완료 처리를 버린 검사**다. 티스토리 서버 내부의 패킷 유실을 재현했다고 주장하지 않는다. E3는 불명확 상태를 먼저 저장한 뒤 종료·재실행한다. 실제 계정에서 저장 클릭 도중 강제 종료한 검사는 아니다.

테스트 DB에만 추가한 SQLite trigger가 커밋된 작업 상태 전이를 기록한다. 운영 테이블/상태 모델은 확장하지 않았다. 공개 이벤트 필드는 익명 jobKey, scenario, 이전/다음 state, 정규화 step, 시각, automaticRetry, existingPostConfirmed, 결과와 순번이다. 대표 전이는 다음과 같다.

```text
queued → running / preparing
       → running / publish_requested
       → unknown 또는 needs_attention
       → queued (명시적 재확인)
       → running / verifying
       → succeeded 또는 needs_attention
```

공개 JSON에는 계정·개인 URL·본문·쿠키·토큰·사진·실제 작업 ID를 넣지 않았다. 실제 URL/원고/작업 매핑은 Git 제외된 격리 DB와 private marker에만 있다. worker 오류 로그도 원문 오류/DOM 저장을 제거하고 익명 키와 상태만 출력한다.

## 자동 검증 결과

외부 저장은 격리 JSON 원장으로 대체하지만, 중복키/임대/claim/복구 상태는 실제 SQLite와 공통 worker 경로를 사용한다. 기존 글 검증은 Playwright가 가로챈 합성 페이지에 **실제 Publisher.verify**를 실행한다. 검증 중 GET 이외 요청은 실패 처리하고 집계했다.

| 시나리오 | 실행/통과 | 최종 상태 | 확인한 결과 |
| --- | --- | --- | --- |
| S1 동시 중복 요청 | 5/5 | succeeded | 별도 프로세스 두 개에서 동시에 enqueue, 같은 ID·글 1개 |
| S2 저장 전 종료 | 5/5 | cancelled | 재시작 후 attention, 미발행 글을 성공으로 오판하지 않음. 명시적 미발행 종료, 글 0개 |
| S3 저장 후 응답 유실 | 5/5 | succeeded | 먼저 unknown 유지·자동 재발행 없음 확인, 후보 읽기 검사 후 완료 |
| S4 URL 후 종료 | 5/5 | succeeded | 같은 ID/스냅샷·URL 유지, 검증 네트워크 오류 거절 후 읽기 복구 |
| S5 URL 없이 불명확 | 5/5 | succeeded | 후보 전에는 자동 재발행·URL 없는 검증 거절, 후보 확인 후 완료 |
| S6 잘못된 후보 | 5/5 | needs_attention | 다른 블로그·다른 주소·제목·본문·공개 상태·카테고리·사진 수·순서 불일치 전부 거절 |

S2의 “안전한 재개”는 기존 정책대로 **확인 절차의 재개**다. 임대 만료만으로 게시 버튼을 다시 누르지 않는다. S3/S5의 최종 성공 전에 unknown/attention 상태의 여러 worker tick과 resume 거절을 검사했다. S6는 반복마다 8개 부정 사례를 묶어서 검사한다.

| 지표 | 자동 모의 | 실제 최종 3개 시나리오 |
| --- | ---: | ---: |
| 중복 작업 | 0 | 0 |
| 중복 게시 | 0 | 0 |
| 불명확 이후 자동 재발행 호출 | 0 | 0 |
| 잘못된 성공 판정 | 0 | 0 |
| 기존 결과 복구 성공 | 15 | 2 |
| 재시작 후 기존 결과 복구 성공 | 10 | 1 |
| 종료 뒤 ID·스냅샷 보존 | 15 | 1 (E3) |
| 생성 글 수 | 모의 25 | 실제 비공개 3 |
| 최종 남은 임대 | 0 | 0 |
| 최종 queued/running | 0 | 0 |

S6의 needs_attention 5건은 의도한 최종 상태다. 실패한 후보를 성공/취소로 정리하지 않았다. S2 cancelled 5건과 succeeded 20건을 포함해 모의 30건 모두 예상 상태다. 실제 검사의 오판 0은 E1~E3에서 관측한 결과이며, 부정 후보 40개의 검증 근거는 모의 S6에 있다.

## 실제 계정 결과와 실패 이력

| 시도 | 결과 | 게시 호출/저장 클릭/글 | 근거 |
| --- | --- | --- | --- |
| E1 | 통과 | 1/1/1 | 고유 제목, 비공개·카테고리·사진 2장/순서·본문 확인, succeeded, 비로그인 본문 접근 차단 |
| E2 | 통과 | 1/1/1 | URL 기록 전 완료 처리 중단 → unknown → idle 확인 → 관리 후보 → 같은 ID 검증 성공 |
| E3 최초 | 실패 | 0/0/미측정 | 테스트 서버 3036 포트와 API의 localhost:3000 보안 규칙 불일치. 게시 전 LIVE_APP_NOT_READY |
| E3 수정 후 | 통과 | 1/1/1 | 같은 queued 작업 이어서 실행. URL 후 attention, 앱/worker 종료·새 PID, API에서 같은 ID/스냅샷 확인, 기존 글 검증 |

실제 최종 검사는 3/3 통과지만 **최초 실패를 0건으로 바꾸지 않았다**. 최초 하네스의 이전 결과 보존 로직에도 결함이 있어 다음 실행에서 실패 항목이 누락됐다. 캡처된 최초 터미널 출력에서 E3 실패·queued·호출 0·클릭 0·글 미측정을 복원하고, JSON의 해당 항목에 근거와 “정확한 실패 시각 미기록”을 명시했다. 이력 누적 로직도 수정했다. 이미 통과한 E1/E2는 다시 실행하거나 게시하지 않았다.

중복 글 수는 각 고유 테스트 제목에 대한 현재 관리 목록에서 확인했다. 전체 계정 이력에 대한 전수 중복 감사는 아니다. 원본 사용자 DB의 기존 작업과 실제 사용자 원고·사진은 수정하지 않았다. 테스트 비공개 글 3개는 검증 근거로 남아 있다.

## 복구 시간

| 구분 | 표본 | 평균 | p95 |
| --- | ---: | ---: | ---: |
| 모의 명시적 읽기 복구 | 15 | 4.461초 | 4.771초 |
| 실제 E2/E3 복구 | 2 | 5.425초 | 5.608초 |

명시적 검증 요청부터 성공 확인까지 측정한다. 초기 발행·장애·임대 만료 대기는 제외한다. 실제 값에는 자식 worker 시작/종료, 관리 목록·본문 검사와 비로그인 접근 확인이 포함된다. 모의 값은 5개 독립 시나리오를 동시에 실행한 환경이다. p95는 nearest-rank 방식이며 실제 표본 2개의 p95는 최댓값이라 일반적인 성능 보장에 사용할 수 없다.

## 재현 명령과 추가 검증

```powershell
npm ci --include=dev
npm run typecheck
npm test
npm run test:publish-recovery
npm run build
npm run test:build-artifacts
npx tsx scripts/web-recovery-check.ts

# 명시적 실제 계정 검사: 전용 Chrome 로그인 및 테스트 카테고리 필요
npm run test:publish-recovery:live
```

단위 테스트 35/35, 타입 검사, 프로덕션 빌드, 빌드 추적 9개 검사, 기존 복구 UI 검사가 통과했다. 사진 식별자 없는 복구 거절은 fail-closed 강화이며 사진이 있는 기존 작업의 호환성에 영향을 준다.

모의 검사는 매번 새 격리 경로에 30회를 실행하고 JSON batch를 추가한다. Chrome이 설치되어 있어야 한다. 실제 검사는 먼저 당일 모의 30/30 통과 결과를 요구한다. 앱 보안 규칙에 따라 localhost:3000이 비어 있어야 한다. `.local/publish-recovery-live`의 안정적인 marker를 재사용하며 성공한 시나리오는 건너뛴다. 중단 작업을 무조건 초기화하거나 marker를 삭제해 재발행하지 않는다. 인증·네트워크·검토 지연이 발생하면 기존 글을 확인하는 경로로 처리한다.

## 한계

- 티스토리의 모든 계정과 스킨에서 검증한 것이 아니다.
- 공개 게시와 예약 게시를 검증한 결과가 아니다.
- 외부 플랫폼에 대한 exactly-once 저장을 보장하지 않는다.
- 중복키가 같은 요청을 억제하고, 결과가 불명확할 때 자동 재발행하지 않는 구조를 검증했다.
- 티스토리 UI가 변경되면 Playwright 선택자와 검증 로직이 영향을 받을 수 있다.
- 모의 장애 테스트와 실제 계정 테스트는 별도로 집계한다.
- 본문 비교는 정규화된 구조/텍스트와 사진 식별 경로를 기준으로 한다. 티스토리 저장 바이트의 완전한 일치, 시각적 동일성, 서버 내부 트랜잭션은 입증하지 않는다.
- 강제 프로세스 종료는 모의 계정에서 검증했다. 실제 E3는 불명확 상태 저장 후 앱·worker 종료/재시작이다.
- 사진 처리 지연, 인증 만료, DB/디스크 손상, 전원 차단, 모든 네트워크 장애 조합, 장기 무인 운영은 별도 검증 대상이다.

