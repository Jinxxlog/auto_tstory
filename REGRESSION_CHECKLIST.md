# R0 회귀 검사와 복구 기준

최종 갱신: 2026-09-23

이 문서는 원고 구조·AI 공급자·블로그 플랫폼을 확장하기 전에 기존 동작이 유지되는지 확인하는 기준이다. 일반 검사는 실제 AI 사용이나 블로그 게시를 하지 않는다. 외부 계정 검사는 이름에 명시된 별도 명령과 사용자 검토로 실행한다.

## 로컬 데이터 보호

| 검사 | 명령 | 통과 기준 |
| --- | --- | --- |
| 데이터 감사 | `npm run audit` | SQLite 정상, 원고/버전 형식 정상, DB 이미지 파일 누락 0건 |
| 백업 생성 | `npm run backup` | `backups/<시각-ID>` 생성, DB·이미지 해시 검증 완료 |
| 백업 목록 | `npm run backup:list` | 손상되지 않은 복원 지점만 날짜·개수로 표시 |
| 백업 검증 | `npm run backup:verify -- <백업 폴더>` | DB 무결성·테이블 개수·DB/이미지 해시 일치 |
| 복원 | `npm run restore -- <백업 폴더> --confirm-replace-local-data` | 웹앱 종료 상태에서만 실행, 복원 전 안전 백업 및 교체 전 데이터 폴더 보존 |

백업에는 원고, 버전, 이미지, 문체, 참고 자료, AI 결과와 작업 이력이 포함된다. `.local`의 티스토리·ChatGPT 로그인 정보와 진단 로그는 포함하지 않는다. 연결 상태와 실행기 임대는 복원 후 다시 확인하도록 초기화한다. 백업 폴더에도 사용자 원문이 있으므로 Git에 올리지 않는다.

복원 절차:

1. `Stop.cmd` 또는 개발 터미널의 `Ctrl+C`로 정상 종료한다.
2. `npm run backup:list`와 `npm run backup:verify -- <폴더>`로 복원 지점을 확인한다.
3. 복원 명령을 실행한다. 기존 DB는 새 안전 백업과 `data.before-restore-...` 폴더로 이중 보존된다. 새 PC처럼 데이터 경로가 없거나 비어 있으면 기존 DB 없이도 가져올 수 있다. DB가 없고 다른 파일이 있는 폴더는 거절한다.
4. `npm run audit`를 실행하고 앱에서 원고 1개를 연다. 개발 의존성이 설치된 개발 환경에서는 `npm test`, `npm run typecheck`도 실행한다.
5. 인증은 복원하지 않으므로 필요한 경우 티스토리·ChatGPT에 다시 로그인한다.

진행 중인 블로그·AI·문체·코드 작업이 있으면 백업 생성을 거절한다. 웹앱 실행기가 켜져 있거나 진행 대기 작업이 있으면 복원을 거절한다. 손상된 manifest, DB 또는 이미지도 복원 후보로 사용하지 않는다.

복원 보존/실패/임시 폴더(`*.before-restore-*`, `*.failed-restore-*`, `.restore-*`)와 임시 백업(`.creating-*`)의 사진은 Git에서 제외한다. 사용자 지정 원본 데이터 폴더와 백업 루트는 사용자가 별도로 제외해야 한다. Windows에서 폴더 이름 변경이 일시적으로 거절되면 최대 4번 추가 재시도하며 계속 실패하면 오류를 유지한다. 복원은 앱 종료 상태에서 수행한다.

## 자동 회귀 검사

- `npm run test:publish-recovery`: S1~S6 각 5회, 격리 DB/프로세스 종료/모의 외부 저장 및 실제 읽기 검증. [상세 결과·실패 이력](BLOG_PUBLISH_RECOVERY_VALIDATION.md).
- `npm run test:publish-recovery:live`: 명시적 실제 계정 비공개 E1~E3. 합성 글을 생성하므로 일반/CI 검사에 포함하지 않는다. 성공 marker는 재사용하고 자동 재발행하지 않는다.


- `npm test`: 원고 버전, 충돌, AI 결과 적용, 문체 스냅샷, 기술·PS 입력, 출처, 코드 실행 제한 설정, 백업·복원·손상 거절을 검사한다.
- `npm run typecheck`: TypeScript 계약과 스크립트를 검사한다.
- `npm run build`: 프로덕션 웹앱 빌드와 라우트를 검사한다.
- `npm run test:build-artifacts`: 빌드 추적 파일에 사용자 데이터·인증·백업/복원 경로가 없음을 검사한다. 설치와 CI도 빌드 후 실행한다.
- `npm run test:writing`: 격리된 로컬 UI/모의 AI 응답으로 여행·정보·자유 입력, 검토 전 생성 차단, 개요/사진 순서, 잠금, 결과 적용, 블록 재작성, 버전/메모 보존, 비공개 큐 스냅샷, 390px 모바일 표시를 검사한다. 외부 계정을 사용하지 않는다.
- `npm run test:blocks`: 별도 합성 DB와 900×600px 이상 단색 PNG 50장으로 업로드·실패 재시도, 10장 교차 미리보기, 코드/표/메모 비게시, 드래그·버튼 순서 변경, 사진 교체, 버전 보존, 모바일 가로 넘침을 검사한다. 고해상도 실사진이나 실제 티스토리 전송 검증과 구분한다.
- `npm run test:installation -- --install`: 허용한 소스만 별도 영문·공백 경로에 설치, 실제 npm ci/build/prune, TypeScript 제거/tsx 유지, 블로그 기본값 없음, 환경 진단, 원고 저장·재시작, 중복 실행, 정상 종료·임대 해제, 작업 실행기 오류 시 웹 서버 종료, 다른 포트 소유자 보존을 검사한다. Chrome 없는 CI는 `--no-browser`로 API 검사를 수행한다. 실제 계정·사용자 자료는 사용하지 않으며 자료는 `.local/installation-*`에 격리한다. 미지원 비ASCII 설치 경로 거절은 단위 검사에 포함한다.
- `npx tsx scripts/web-recovery-check.ts`: 빌드 후 빈 3000 포트에서 별도 Next 서버·합성 DB로 복구 UI/요청을 검사한다. 작업 실행기는 켜지 않는다. 외부 요청을 가로챈 모의 티스토리에서 제목/주소·본문·카테고리·비공개 검사, 다음 목록 페이지 탐색과 쓰기 요청 없음도 확인한다. 실제 티스토리 검증은 아니다.
- `npm run test:web`: 로컬 웹앱에서 원고·사진 2장·캡션·순서·재편집·코드·표·모바일 표시를 검사한다. 테스트 원고를 로컬 DB에 추가하며 블로그에는 게시하지 않는다.
- `npx tsx scripts/web-assets-check.ts`: 이미지 라이브러리·폴더 가져오기·순서·캡션·요청 보호를 검사한다. 블로그에는 게시하지 않는다.
- `npx tsx scripts/web-content-check.ts`: 기술·PS 입력·자료·예제·모바일 표시와 Docker 미준비 상태를 검사한다. `--generate`는 ChatGPT 한도를 실제 사용하므로 평상시 회귀 검사에서 제외한다.

## 계정·외부 서비스 회귀 검사

실제 계정 상태나 외부 서비스 화면이 바뀐 뒤에만 수행한다. 새 글을 저장하는 검사는 제목과 대상 카테고리를 먼저 확인하고 비공개를 기본값으로 사용한다.

- 티스토리 연결: 전용 Chrome 직접 로그인, 카테고리 목록, 세션 재시작 후 복원.
- 티스토리 전송: 확정 원고 버전, 비공개, 카테고리, 사진 순서·캡션·대표 이미지, 코드·표를 다시 열어 확인.
- 저장 직후 중단: URL이 있으면 `unknown`에서도 같은 글만 재확인한다. URL이 없으면 관리 화면에서 찾은 글을 후보로 입력하고 검증 후 연결한다. 사용자가 미발행을 확인하면 작업만 종료하며 새 글을 자동 생성하지 않는다.
- ChatGPT: 계정 연결·모델 목록·한도 표시, 생성 결과 별도 보관, 원고 변경 후 오래된 결과 적용 거절, 취소 후 늦은 결과 미적용.

## 현재 알려진 미해결 항목

- P1 비공개 글의 두 번째 사진은 티스토리 검토 안내 이력이 있어 기존 글에서 최종 표시를 다시 확인해야 한다.
- Docker가 설치되지 않아 Python 예제·경계 사례의 실제 컨테이너 실행과 시간·메모리·출력 제한은 런타임 검증 전이다.
- 장기 로그인 유지, 다른 티스토리 스킨·계정·중첩 카테고리는 검증 전이다.
- 공개 발행, 예약, 네이버 블로그, Gemini는 [2차 로드맵](ROADMAP_V2.md)의 후속 단계다.
- R0.1은 합성 상태 전이·격리된 복원·로컬 UI/모의 게시글 검사까지 통과했다. 강화된 재확인 로직의 실제 티스토리/다른 스킨 검증은 별도로 수행해야 한다. 관리 목록은 실제 페이지 링크를 최대 20쪽까지만 읽고 못 찾으면 확인 대기로 남긴다.
- R7.1은 같은 PC의 격리된 새 소스 폴더에서 실제 설치와 배포 실행을 검증했다. 타 PC·업데이트/롤백·GitHub 원격 CI 실행은 미검증이며, 한글 등 비ASCII 설치 경로는 현재 거절한다.
- R2a/b 구현과 로컬 회귀는 통과했으나, R2c 실제 10장 티스토리 비공개 저장·재열기는 재로그인 대기다. 새로운 AI 블록 출력/사진 분석의 실제 계정 검증도 아직 수행하지 않았다. 이전 앱으로 되돌릴 때는 R2 이전 백업을 사용한다.

## R3 실제 합성 인수 명령

- `node --import tsx scripts/r3-private-check.ts --generate`: ChatGPT 구독으로 합성 3유형 초안과 사진 블록 재작성. 실제 사용자 원본 DB와 분리하고 결과를 `.local/r3-private/review.json`에 저장한다.
- 결과 검토 후 `node --import tsx scripts/r3-private-check.ts --private-test`: 테스트 카테고리에 실제 비공개 글 3개 저장·재열기·비로그인 검사. 이미 저장된 URL은 조회만 하며 URL 없는 중단은 자동 재전송하지 않는다.
- `node --import tsx scripts/r3-private-check.ts --verify`: 같은 3개 글 읽기 전용 재확인.
- 2026-09-23 실제 합성 인수와 R2 10장 같은 글 재확인까지 통과. 사용자 실자료·문체·다른 계정/스킨은 성공 범위에 포함하지 않는다.
